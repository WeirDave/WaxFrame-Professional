# Builder Console: Cleaner Selected Card

This is a tiny visual cleanup for Setup Step 2.

---

## What Changed

**Removed the small Builder bee from the selected Builder card**

The page already has the full Builder mascot in the header. The duplicate inside the selected Builder card was too small to read well, so it has been removed.

**The Builder Console layout stays the same**

The selected Builder panel, model selector, available Builder roster, long-name handling, and responsive laptop behavior from v3.63.69 are unchanged.

---

## Files Changed

- `js/app.js`
- `style.css`
- `js/version.js`
- `CHANGELOG.md`
- `docs/WaxFrame_Backlog_Master_v136.txt`
- Release stamp sweep across all HTML, JavaScript, and CSS source files
