WaxFrame Backlog Master v222
2026-06-04

State: v3.63.153 Pro shipped on main. Biggest Worker Bees rework release of the evening — three structural shifts in one ship so the screen finally matches worker-bees-prototype.html v2.2 layout:

1. Sidebar chassis. New .bees-layout 2-column flex inside hp-section-body. Left column (.bees-sidebar, 240px sticky) carries Icon Legend (✨🔨💰⚖️🧠⚡), Jump-to-AI nav (alphabetized within Default/Custom subheads, green ● for AIs with keys), and Related links (API Key Guide / AI API Pricing). renderHiveSidebar() builds it; jumpToAISetupRow() handles click-to-expand-and-scroll with 80px header offset. Hidden in Server mode. Stacks below 1100px.

2. Collapsed-row redo. Model dropdown moved OUT of expanded panel and INTO the row header (David's prototype-matching request). New order: chevron → icon → name-group → "✓ Ready" status pill (or "✗ Invalid" red pill) → compact native <select> → ⚠ deprecation flag → spacer → action. Select has emoji badges baked into option text (✨🔨💰⚖️🧠⚡), onchange routes through saveModelForAI + renderAISetupGrid, onclick stop-propagates. Saved-but-not-in-list models surface as "(saved — not in current list)" first option.

3. Color cohesion. is-active state moved from GREEN to GOLD (var(--accent)) across .ai-hive-card.is-active, .ai-hive-card-pill, .ai-builder-affordance.is-active, .ai-setup-builder-chip. Green is now SEMAPHORE-ONLY: status / health / validation (✓ Ready pill, ✓ key valid card edge). Gold is selection / brand accent. Test + Recommend Models buttons stay blue as a coordinated utility pair per the v3.32.12 design note (David caught this when I was about to recolor them gold — they're meant to be paired).

Plus: whole-card click on the 6 tier/role cards (lost the inner "Use this" button; entire card is the target with role="button" + tabindex + Enter/Space handlers). API key input now spans full row width for ChatGPT's long sk-proj-… keys. Expanded panel's old modelSelector replaced with a tight .ai-recommend-row containing the Recommend Models button + status line.

Transparent Builder Bee v4.png — David fixed transparency on the asset (was 793 KB, now 923 KB). Same filename so no markup changes.

David's three observations that drove this release:
- "there's just a lot of green on the dark side" (color)
- "hover is like what we do for buttons and nothing happens when you click the cards" (interaction)
- "I liked it initially but your cards were a lot smaller than because you had the whole side rail over there which you don't currently have" (structure)

All three landed in this release.

David also explicitly removed the "ask which next?" friction earlier in the session — captured in [[feedback-make-the-call-dont-ask]]. The recoloring of the Recommend Models button (which I was about to do gold but he stopped me) is the canonical example of why that memory IS scoped to "ordering of work within a settled scope" rather than "any judgment call": when scope is genuinely ambiguous (or when there's a non-obvious design rationale he holds and I don't), redirection is fine.

v3.63.152 state still holds: Hive Profile dropdown above bee grid (5 options: 🛠 Custom / 💰 Cheap / ⚖️ Balanced / 🧠 Thinker / ⚡ Fast). v3.63.151 state still holds: 6-card grid in expanded row. v3.63.150 state still holds: collapsed-row name-group cleanup.

PRIOR STATE (v3.63.152, retained for context until next release reshuffles the margin):
Hive Profile dropdown. applyHiveProfile walks aiList + uses getCachedTiers to set models. Persisted to localStorage["waxframe_hive_profile"]. Internet mode only.

PRIOR STATE (v3.63.151, retained for context until next release reshuffles the margin):
6-card grid (Role + Tier picks) in expanded row. Closes #22. Built on existing getReviewerRecommendation / getBuilderRecommendation / getCachedTiers.

PRIOR STATE (v3.63.150, retained for context until next release reshuffles the margin):
Worker Bees collapsed-row cleanup. .ai-setup-name-group inline-flex with gap:4px. Pencil button padding-x and margin-left zeroed.

Open Bugs

(none currently tracked outside the active task list)

Open Features / Tasks — Worker Bees rework Phase 4+

- #10 Hive Profiles: "Save current as named profile" (named user profiles); SHOULD wait for delta-tracking UX
- #12 Hive Profiles: dedicated docs page (hive-profiles.html)
- #24 Worker Bees rework: drop sub-1366px responsive code — cleanup pass now that the sidebar landed; design is settled
- #26 Worker Bees rework: toolbar alignment — Recommend All AIs button — partially covered by v3.63.152/153; remaining: visual alignment between profile bar + controls row
- #27 Worker Bees rework: 3-group ordering (defaults / customs / servers) — current Internet/Server toggle means defaults+customs share a page, servers another; prototype's single-page 3-group view is a bigger restructure
- #32 Builder consolidation: documentation sweep (User Manual, README, landing pages, info modals)
- #34 Worker Bees rework: Builder education in 3 surfaces — modal + standalone page remaining
- #35 Auto-classify tier picks always (not click-to-classify)
- #36 Worker Bees rework: server provider group label
- Per-row "Manual override" tag — needs delta tracking; deferred from v3.63.152
- Per-tier WHY text: classifier currently produces one overall WHY; could split per-tier
- WaxFrame_Builder_v3.png pruning candidate once worker-bees-prototype.html is updated or retired
- Restore-from-Checkpoint diff-and-pick UI — PARKED
- Hive parallel fan-out RPS-awareness
- Model recommender filter-awareness warning
- Failover Builder behavior — single-round CONTENT_FILTERED handling
- Builder-incapable family list expansion
- Pause/resume at reviewer seam
- File System Access API exploration
- Provider Catalog foundation (parked)
- CSS dead-selector cleanup branch (now includes the obsolete .ai-setup-model + .ai-hive-card-action rules post-v3.63.153)
- CSS rule consolidation sweep
- Repo housekeeping — docs/ folder
- Triage CodeQL + Dependabot findings
- Rules reference update — "verify every external URL before shipping" + "always update const BUILD in addition to the // Build comment" + "release-check validator should compare const BUILD against // Build comment"

Observations

- 2026-06-04 — v3.63.153 lands the largest Worker Bees rework piece of the night because everything I'd shipped earlier (Phase 1 cards, Phase 2 profile bar) was structurally constrained by the missing sidebar. David's instinct ("cards were smaller because you had the side rail") was correct — without the sidebar the main column inherited the full screen width, which made the 6-card row look stretched and small-text-y at the same time. Adding the sidebar back is what makes the prototype's design land.

- 2026-06-04 — David's recoloring redirection (blue Recommend Models stays paired with blue Test) was a high-value correction. I'd staged it for repaint without remembering the v3.32.12 design note that the pair is intentional. Captured the rule above in the CHANGELOG so it's discoverable on subsequent passes.

- 2026-06-04 — Worker Bees v3 rework arc in one day: v3.63.147 (core consolidation, Builder screen retired) → v3.63.148 (mascot placement) → v3.63.149 (mascot asset swap) → v3.63.150 (row cleanup) → v3.63.151 (6-card grid) → v3.63.152 (Hive Profile dropdown) → v3.63.153 (sidebar + collapsed redo + color cohesion). Seven releases in one evening, each landing a specific piece of feedback. The arc demonstrates that David's tight feedback loop + willingness to redirect mid-build produces clean cumulative releases.

- 2026-06-04 — release-check validator caught no drift across v3.63.135 through v3.63.153 (nineteen ceremonies).

- 2026-06-04 — SEO measurement infrastructure live since v3.63.128.

- cleaner.html preserved in David's local working tree.

- Adoption-friction note: David surfaced "zero sales after months of build" 2026-06-01. The day's seven-release arc (v3.63.147–v3.63.153) is dominantly focused on conversion-friction reduction. Each release lowered cognitive load at a specific point in the user's first-time path. Together they should significantly improve "I want to try this → I'm refining a document" conversion.
