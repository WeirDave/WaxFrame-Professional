WaxFrame Backlog Master v202
2026-06-04

State: v3.63.133 Pro shipped on main. Deep Dive capture dual-homed onto Settings → Diagnostics + a one-click "Enable Deep Dive" button on help.html. Dev-toolbar 🔬 toggle kept for fast access. All three surfaces sync the same localStorage flag via WF_DEBUG.setDeepDive() — no drift. New Settings → Diagnostics section (index.html ~1049) sits between Workflow & Updates and Storage; has a Deep Dive toggle + View Captures action button. renderSettings() in js/app.js syncs the toggle's checked state from WF_DEBUG.deepDiveOn when Settings opens. WF_DEBUG.setDeepDive() now also flips #setDeepDive in addition to the existing #wfDeepDiveToggle. help.html has a new Step 2 in the bug-report flow ("Optional: enable Deep Dive first for richer data") — button sets localStorage.waxframe_deepdive='1' directly (no need to load wf-debug.js on help.html; localStorage is shared across tabs of the same origin so the next WaxFrame open / refresh picks it up). State reflected on load — if already on, the button shows "✓ Deep Dive already enabled" with next-step instructions. No behavior change to in-app Deep Dive mechanics (same ring buffer 200 entries, same schema, same viewer, same redaction in the diagnostic bundle). Pure surfacing change.

REMOVED FROM BACKLOG (per David's 2026-06-04 confirmation):
  • importSession ID-normalization bug. With zero confirmed downloads / zero sales, no user-in-the-wild has a checkpoint file that could carry the legacy ugly IDs. The bug's exposure surface is currently zero AND will stay zero unless and until actual users surface with their own legacy backups. David is the only person with legacy data and he can clean his own files via cleaner.html (the offline tool that already validated 64/68 cleanup against his pile). Re-add the entry if/when a real user reports the symptom — the cleaner.html reference implementation is preserved in David's local working tree.

v3.63.132 state still holds: Builder-incapable model filter (Jamba) + BUILDER_TRUNCATED error code + docs. v3.63.131 state still holds: audit polish. v3.63.130 state still holds: selective EXPORT modal + scrubbed retirement. v3.63.129 state still holds: Backup → Checkpoint rename. v3.63.128 state still holds: 17-playbook body-copy link sweep. v3.63.127 state still holds: deep-dive cross-link reframe. Sitemap lastmod 2026-06-04.

PRIOR STATE (v3.63.132, retained for context until next release reshuffles the margin):
Builder-incapable model filter (Jamba) + BUILDER_TRUNCATED error code + docs that thwart wasted API credits. New BUILDER_INCAPABLE_FAMILIES regex `/^(jamba|ai21[._-]?jamba)/i` in js/app.js wired into filterModelsForRole() so the recommender skips Jamba for Builder. New isBuilderIncapableModel(m) helper. ⚠️ Reviewer-only badge in buildModelSelector() with `.is-builder-warn-pick` row tint via existing --warn token. BUILDER_TRUNCATED entry in WF_ERROR_CATALOG matching ctx.kind === 'builder_truncated'; new `truncated` entry in showRoundErrorModal's ctxKindMap; detection in BOTH runRound's Builder check (~13713) AND runBuilderOnly's Builder check (~14691) — finishReason LENGTH/MAX_TOKENS routes to BUILDER_TRUNCATED. ai-api-pricing.html new amber FAQ "Can AI21 Jamba be used as a Builder?" with structural explanation + 4096-cap citation, mirrored in JSON-LD. waxframe-user-manual.html Change Builder description documents the badge; structure-error troubleshooting expanded to two-bullet diagnosis (truncation vs ignored-instructions).

PRIOR STATE (v3.63.131, retained for context until next release reshuffles the margin):
Audit polish on v3.63.130. (1) `.save-checkpoint-modal` --modal-w-md → --modal-w-base (token didn't exist). (2) `.checkpoint-scope-row-child` --space-22 → --space-24 (token didn't exist). (3) Removed dead `let leftLocalIDBAlone = false;` from importSession. (4) waxframe-user-manual.html #settings anchor for "Save Checkpoint scope" reworded to #checkpoint-scope (with id added to target wh-block).

PRIOR STATE (v3.63.130, retained for context until next release reshuffles the margin):
Phase B of the Checkpoint arc — selective EXPORT modal + retirement of the Save Checkpoint (Scrubbed) flow. 💾 Save Checkpoint nav item opens a modal with six checkboxes: Project / Session / Hive (parent) / API keys / Builder / License. Backup format v4 → v5 with new _waxframe_backup_scope envelope. importSession's v5 path treats null + scope=false as "preserve local". Retired: 🧼 Save Checkpoint (Scrubbed) menu item, backupSessionScrubbed() (~65 lines), scrubBackup() helper (~50 lines), _waxframe_backup_scrubbed import branch (~25 lines), Scrubbed-checkpoint section in user manual.

PRIOR STATE (v3.63.129, retained for context until next release reshuffles the margin):
Phase A of the Checkpoint arc — vocabulary rename "Backup" → "Checkpoint" and "Backup Builder" → "Failover Builder" across every user-facing surface. Internal code names stay. Filename stems: -WaxFrame-Backup- → -WaxFrame-Checkpoint-. Existing -WaxFrame-Backup-*.json files still restore correctly via the _waxframe_backup: true format guard.

PRIOR STATE (v3.63.128, retained for context until next release reshuffles the margin):
Finished the v3.63.108 helper-page body-copy linking sweep — the remaining 17 of 22 document-playbooks.html playbooks now carry the "Apply it directly in WaxFrame from the X template ↗" body-prose link. ALL 22 playbooks have the same shape. Google Search Console + Bing Webmaster Tools setup done in this same session. SEO measurement infrastructure now live.

PRIOR STATE (v3.63.127, retained for context until next release reshuffles the margin):
Reframed cross-links to the three use-case landing pages as "deep dives" instead of "guides" / "short versions." templates.html LANDING_PAGES map labels each card "🔍 Deep dive: …"; document-playbooks.html three playbook descriptions reframed to "For a deep dive on why a multi-AI hive beats one AI here — [page-specific specifics] — see the … deep dive ↗".

Open Bugs

(none currently tracked — see REMOVED FROM BACKLOG note in State above for the importSession ID-normalization entry's deletion rationale)

Open Features

- Restore-from-Checkpoint diff-and-pick UI — turn the current blunt overwrite into a comparison + selective-import flow. CURRENT (post-v3.63.130): selective EXPORT decides which sections leave the source machine; selective restore happens automatically via the v5 scope flags. But the receiver still has no preview of WHAT will change before commit. PROPOSED: after the trust-warning, before any writes happen, show a comparison modal that diffs checkpoint vs current local state across the same six sections the EXPORT modal exposes plus per-AI key diff and per-AI model-selection diff. STATUS 2026-06-04: PARKED — bigger scope, gated on user feedback once the selective-export modal has actual users.

- Custom Templates Phase 2 + Phase 3 — full editors behind the scaffold buttons that landed in v3.63.97 on templates.html. PHASE 2 (Create Blank): guided multi-step editor. PHASE 3 (Import .json): file-picker that validates and dedupes against existing customs. PHASE 1B — hive auto-reconcile on apply — separate concern; when a custom template's captured hive includes AIs the user no longer has configured, today the apply silently drops them.

- Templates page Phase 1 follow-ups (post-v3.63.97 ship) — small refinements: search/filter bar, sort options on My Templates, per-card View Details expander, per-card duplicate action on customs, bulk Export All customs as ZIP.

- Mistral 429 proactive warning via X-RateLimit-Remaining header. Mistral's API returns the header; WaxFrame doesn't currently read it. Surface a soft warning BEFORE a 429 hits (e.g. yellow chip on the Mistral row "~X requests left this minute"). Pairs with the API console URL split. Likely needs a small per-provider hook in callAPI (app.js ~11500) since rate-limit headers differ by provider.

- Hive parallel fan-out RPS-awareness. Reviewer rounds fire up to N concurrent calls. With paid Mistral on Tier 1 or 2, that parallel burst itself can trigger 429. Optional per-provider concurrency cap. UI: a Settings toggle "Throttle Mistral to 1 RPS (paid tier compatibility)" — off by default.

- Model recommender filter-awareness warning — when the recommender's pick has been flagged in the current session's ringBuffer with CONTENT_FILTERED, surface a ⚠️ next to that model in Change Builder and in the recommend toast. Self-learning from actual session history. Doesn't block the pick.

- Failover Builder behavior — primary Builder hits CONTENT_FILTERED / EMPTY_RESPONSE → auto-failover to a configured Failover Builder for that one round, then snap back to primary next round. Settings → Auto Mode already has the dropdown; this entry is about the BEHAVIOR — today the dropdown only fires on failure-streak halts, not single-round CONTENT_FILTERED.

- Builder-incapable family list expansion. v3.63.132 hardcoded `/^(jamba|ai21[._-]?jamba)/i` — currently just Jamba. As other broken-as-Builder families surface, extend the regex. The self-healing quarantine (v3.63.31 path) still catches one-off failures. WATCH-LIST — no active work until a real failure surfaces.

- Auto-quarantine on BUILDER_TRUNCATED. v3.63.132 added the error code but did NOT wire it into the self-healing quarantine. Argument for: structural incapacity for a user's typical document length. Argument against: one bad round permanently blocking a model is aggressive. DEFAULT: leave manual.

- Legacy ID Tier 2 migration helper. Tier 1 (inline rename pencil) shipped v3.63.96. Tier 2 would rewrite legacy IDs across all storage locations. Higher risk; defer.

- Amazon Product Review canonical length recipe (reference data for templates.js):

  Amazon: Min 500 chars / Normal 1,500–3,500 / Detailed 4,000–7,500 / Hard cap 20,000 (4K recommended)
  Best Buy: 5,000 chars hard cap
  Target / Walmart: 4,000 chars hard cap
  Newegg: 5,000 chars hard cap
  Manufacturer site / blog: no limit

- Product Review per-platform templates — extend the per-platform pattern (established v3.63.94 with product-review-amazon). Pending: Best Buy, Walmart, Target, Newegg. Each platform gets its own template, not an overlay on a generic one.

- GitHub Actions release-ceremony consistency check — workflow under .github/workflows/ that runs `node --check` on all JS, verifies the four version stamps match across files (meta waxframe-build / APP_VERSION / BUILD / app.js?v=), validates no dead local asset links, AND checks for non-existent CSS token references (the v3.63.131 audit caught --space-22 and --modal-w-md typos that silently failed). Automates the manual release verification. NEXT UP — scoped for v3.63.134.

- Pause/resume at reviewer seam — finer-grained pause point INSIDE a round, between reviewers finishing and the builder firing. Current pause/halt mechanics all operate at round boundaries.

- File System Access API exploration — replace download-only checkpoints with true file persistence (showSaveFilePicker / showOpenFilePicker / FileSystemHandle). Chrome/Edge supported; Safari/Firefox not.

- Provider Catalog + Hive Profiles Foundation — two-layer provider model. Collapses "default six" concept via `starter: true` flag on catalog entries (replaces knownDefaultIds array). Sequencing: (1) starter flag, no UI change. (2) "+ Add from Catalog" modal on Worker Bees. (3) Catalog expansion in two batches — Cloud API (Perplexity, Cohere, Together) and Local Server (LM Studio, Ollama, Alfredo, Open WebUI). (4) Hive Profiles as saved snapshots. Catalog schema carries `kind`: cloud / local-server / router-aggregator. Hard rule: "known AND verified end-to-end," not "exists in the world."

- CSS dead-selector cleanup branch — ~155 flagged selectors, some confirmed live. Static grep insufficient: display:contents is load-bearing; JS template-literal class names evade grep. Dedicated branch + visual smoke test as pre-deploy gate.

- CSS rule consolidation sweep — Codex audit flagged 6 targets: .btn-danger + .license-modal-btn-danger; #wfConfirmOkBtn.wf-confirm-danger + .length-guard-continue; .length-guard-indicator + .slow-responder-indicator; .work-line-numbers + .proj-ta-nums; .custom-ai-modal-overlay + .license-modal-overlay; .btn-accent:hover + .footer-btn-smoke:hover.

- Repo housekeeping — docs/ folder holds older backlog versions, prior playbooks, audit files, prompts reference. ZIP ships latest backlog + Rules Reference + 3-version rollback margin only.

- Triage CodeQL + Dependabot findings once first scans complete. Blocked on David pulling Security tab findings.

Observations

- SHIPPED — Deep Dive dual-homing (v3.63.133, this release). Settings → Diagnostics + help.html Step 2 button both flip the same localStorage flag the dev-toolbar 🔬 toggle uses. All three surfaces stay in sync via WF_DEBUG.setDeepDive(). Closes the "Move Deep Dive toggle off the dev toolbar" Open Features entry.

- SHIPPED — Builder-incapable model filter (Jamba) + BUILDER_TRUNCATED error code (v3.63.132). Three layers of guard against wasted API credits on Jamba-as-Builder: filter at recommendation time, badge at selection time, truncation card at failure time. Confirmed against AI21 docs / AWS Bedrock / OpenRouter June 2026 — 4096-token output cap holds across Jamba 1.5/1.6/1.7. Closes the v200 "Builder-capability filter" Open Features entry.

- SHIPPED — v3.63.130 audit polish (v3.63.131). Four small issues caught in a self-audit pass before any users hit them: --modal-w-md (didn't exist), --space-22 (didn't exist), leftLocalIDBAlone dead variable, misdirected anchor link. The CSS-token check was the most useful — it caught silent fallbacks the browser would never warn about at runtime. Motivates the v3.63.134 GitHub Actions validator (next release).

- SHIPPED — Selective EXPORT modal + scrubbed-checkpoint retirement (v3.63.130).
- SHIPPED — Backup → Checkpoint rename + Failover Builder rename (v3.63.129).
- SHIPPED — Helper-page body-copy linking continuation (v3.63.128). 22/22 playbooks have the template-anchor link.
- SHIPPED — Current API Pricing page. ai-api-pricing.html live + backed by a Cloudflare Worker + KV.

- 2026-06-04 — SEO measurement infrastructure live since v3.63.128. GSC Domain property waxframe.com verified, sitemap submitted, Bing Webmaster Tools imported. No client-side analytics (landing pages explicitly promise none). First signals expected ~2 weeks from setup.

- 2026-06-04 (this session) — six releases shipped in one day: v3.63.128, .129, .130, .131, .132, .133. Each followed full ceremony: APP_VERSION + build stamp + cache-bust sweep + package.json + JSON-LD softwareVersion + CHANGELOG prepend + backlog rotation + commit + push origin HEAD:main + GitHub release. v3.63.134 (GitHub Actions release-ceremony validator) queued as the next release, will help prevent typo regressions going forward.

- cleaner.html (offline backup-cleaner tool) exists as a working reference implementation for the eventual in-app normalizeHive() port — IF a real user ever surfaces with legacy-ID checkpoint files. Lives in David's local Dropbox, NOT committed. 2026-06-02 audit: 68/68 parsed, 64/68 cleaned, 0/68 collision damage; 4/68 outliers had Anduril/Alfredo work-gateway custom AIs correctly skipped. Stays as on-the-shelf reference; the in-app port + the importSession ID-normalize fix are both BOTH explicitly DEFERRED (now also removed from the active bug list).

- Single-branch workflow + push-to-main + GitHub Release ceremony confirmed 2026-06-04: WaxFrame ships everything direct to main via `git push origin HEAD:main`, no feature branches kept around. Worktrees create local claude/<name> branches as a side effect; cleaned up at session end. Every push followed immediately by `gh release create vX.Y.Z --target <full-hash> --notes-file <changelog-section>` — release ceremony isn't complete after the push alone. Memory updated.

- Adoption-friction note (session context): David surfaced "zero sales after months of build" anxiety 2026-06-01, reaffirmed 2026-06-04. Architectural decisions should NOT be re-litigated on sales speculation. Practical implication: zero-known-users means it was safe to retire the scrubbed-checkpoint import branch in v3.63.130, safe to ship the Jamba filter in v3.63.132 without per-user override flags, and safe to delete the importSession ID-normalization bug entry in v202.

- Dead localStorage flag: waxframe_suppress_sensitive_backup_confirm. v3.63.130 removed the wfConfirm it was a suppression flag for. Any users with the key set will have a harmless orphan in localStorage. Not worth a cleanup migration.
