WaxFrame Backlog Master v226
2026-06-04

State: v3.63.157 Pro shipped on main. Eleventh Worker Bees release of the evening. Polish pass + finalization of in-app "Set up your hive" rename:

1. Card-click bug FIXED in v3.63.156 via the explicit `window.swapAIModelFromHiveCard = ...` assignment. David confirmed working. Bare top-level `function foo()` declarations weren't being globally resolved for inline onclick attributes in his runtime. Worth keeping as the canonical pattern. v3.63.157 removes the diagnostic console.log and applies the same window-binding to setBuilder as belt-and-suspenders.

2. Builder button click no longer expands the row. Previous onclick was `setBuilder; stopPropagation; renderAISetupGrid` — stopPropagation ran AFTER setBuilder, so any throw inside setBuilder would bypass the stop. Reordered: stopPropagation + preventDefault + return false FIRST.

3. Builder button active state uses light-tint treatment (rgba(244,180,0,0.12) + gold border + gold text) mirroring the .ai-setup-status-pill.is-ready (green) palette. Was solid gold + dark text. David: "instead of being filled in it should just be lightly filled in like the ready button".

4. Custom AI count: gold (var(--accent)), no parens. Earlier today swung between bubble badge → "(4)" parens (v3.63.154) → muted white (v3.63.157 first attempt) → final landing: gold inline number, no parens. David's call: numbers in gold read as numbers and don't need parens.

5. Hive Profile "Custom" note rewording. Was "Currently using your manual picks. Switch profile above to auto-set tier-matched models." — but the dropdown IS the profile picker, there's no "above". New: "Each AI is using a manually-picked model. Pick a tier from the dropdown to apply one across the whole hive."

6. Hive Profile note wrap behavior: max-width 420px → 560px, added text-wrap:balance so lines distribute evenly instead of breaking mid-clause.

7. In-app nav menu rename: Welcome CTA tooltip + back-button tooltips + nav menu item label + nav menu tooltip all changed from "Worker Bees" to "Set up your hive" (the entity name "Worker Bees" stays everywhere it refers to the AI helpers themselves). User Manual + README + landing pages NOT in this release — that's the bigger doc sweep #32.

Pattern note for future inline-onclick functions: always assign explicitly to window. `window.foo = foo;` after the function declaration. Bare `function foo()` should work in standard <script> tags but David's runtime proved it doesn't always. Worth a release-check rule: any function called from an inline onclick attribute should have a corresponding window.fn = fn binding.

v3.63.156 state still holds: Recommend All keepExpanded, Jamba/Together stale-dropdown fallback, card-click diagnostic+fix. v3.63.155 state still holds: per-row Builder button, Manage link, expanded panel simplified.

PRIOR STATE (v3.63.156, retained for context until next release reshuffles the margin):
Three bug fixes. Recommend Models for All keepExpanded:false. _buildCompactModelSelect renders .is-stale fallback when no live model list. Card-click diagnostic with explicit window binding (which turned out to BE the fix).

PRIOR STATE (v3.63.155, retained for context until next release reshuffles the margin):
Per-row Builder button on every keyed row. Manage link in collapsed row. Expanded panel simplified.

PRIOR STATE (v3.63.154, retained for context until next release reshuffles the margin):
Column alignment via fixed widths. Builder chip moved out of name-group. "Model:" label. Bulk-select inside Custom AIs header. Page renamed "Set up your hive" (H2 title).

Open Bugs

(none currently tracked outside the active task list)

Open Features / Tasks — Worker Bees rework Phase 8+

- #10 Hive Profiles: "Save current as named profile" (named user profiles)
- #12 Hive Profiles: dedicated docs page (hive-profiles.html)
- #24 Worker Bees rework: drop sub-1366px responsive code — cleanup pass; design is settled
- #27 Worker Bees rework: 3-group ordering (defaults / customs / servers) — bigger restructure
- #32 Builder consolidation: documentation sweep (User Manual numbering + Builder screen removal + Set up your hive rename across landing pages + README) — partially done (in-app surfaces done in v3.63.154 + v3.63.157)
- #35 Auto-classify tier picks always (not click-to-classify)
- #36 Worker Bees rework: server provider group label
- Per-row "Manual override" tag — needs delta tracking
- Per-tier WHY text: classifier currently produces one overall WHY
- Dead-CSS cleanup pass: legend-block from v3.63.154, builder-chip from v3.63.155
- WaxFrame_Builder_v3.png pruning candidate (worker-bees-prototype.html still references it)
- Restore-from-Checkpoint diff-and-pick UI — PARKED
- Hive parallel fan-out RPS-awareness
- Model recommender filter-awareness warning
- Failover Builder behavior — single-round CONTENT_FILTERED handling
- Builder-incapable family list expansion
- Pause/resume at reviewer seam
- File System Access API exploration
- Provider Catalog foundation (parked)
- CSS rule consolidation sweep
- Repo housekeeping — docs/ folder
- Triage CodeQL + Dependabot findings
- Rules reference update — "verify every external URL before shipping" + "always update const BUILD in addition to the // Build comment" + "release-check validator should compare const BUILD against // Build comment" + "inline-onclick callable functions need window.fn = fn binding"

Observations

- 2026-06-04 — The card-click bug closes a six-release saga (v3.63.151 introduced the cards, .153 made the whole card clickable, .154 tried data-attribute fix, .155 tried inline-escape, .156 added diagnostic + explicit window binding which turned out to be THE fix, .157 production-cleans the diagnostic). The lesson: trust user reports of "click does nothing" even when the rendered HTML looks correct. The bug was specifically that bare function-declaration global binding wasn't resolving for inline onclick attributes — a JS runtime quirk that's not catchable by static analysis or unit tests.

- 2026-06-04 — Worker Bees v3 rework now eleven releases deep (v3.63.147 → v3.63.157). David's design feedback continues to drive each iteration. The Builder button's design has gone through three iterations alone: standalone screen → expanded-panel affordance → per-row button (initially solid gold, now light-tint). Each iteration was driven by David seeing the prior version live.

- 2026-06-04 — release-check validator caught no drift across v3.63.135 through v3.63.157 (twenty-three ceremonies).

- 2026-06-04 — SEO measurement infrastructure live since v3.63.128.

- cleaner.html preserved in David's local working tree.

- Adoption-friction note: David surfaced "zero sales after months of build" 2026-06-01. The Worker Bees arc is producing visible cumulative polish — column alignment, light-tint states, consistent count styling, intuitive interaction (cards click; profiles apply; rows stay collapsed during bulk ops). Each release smooths a friction point. Conversion impact remains to be measured.
