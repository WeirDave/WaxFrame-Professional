WaxFrame Backlog Master v196
2026-06-03

State: v3.63.127 Pro shipped on main. Reframed cross-links to the three use-case landing pages as "deep dives" instead of "guides" / "short versions." David flagged that the v3.63.123 framing on both templates.html and document-playbooks.html misrepresented what the landing pages are — they aren't a shorter version of the playbook, they're the WHY (screening reality + multi-AI rationale + origin story + what gets edited and what doesn't + what a typical session looks like). Two surfaces fixed: (1) templates.html LANDING_PAGES map in renderTemplateCard now labels each card button "🔍 Deep dive: AI Cover Letter Editor →" / "🔍 Deep dive: AI Résumé Review →" / "🔍 Deep dive: AI Business Proposal Writer →" — the magnifying-glass emoji signals depth-of-context, the explicit page name tells you where the link goes. (2) document-playbooks.html three playbook descriptions reframed from "For the short version of this workflow, see the … guide ↗" to "For a deep dive on why a multi-AI hive beats one AI here — [page-specific specifics] — see the … deep dive ↗" (cover letter: application-portal screening reality + origin story; résumé: ATS/copilot screening reality + origin story; business proposal: how each model's blind spots show up in proposals + typical session). Pure cross-link copy reframe. No behavior change, no new pages. v3.63.126 state still holds: cover letter page lead reframe + matching origin story. v3.63.125 state still holds: résumé page same reframe. v3.63.124 state still holds: Tools alphabetical, ⏱ Round History. v3.63.123 state still holds: three use-case SEO landing pages. Sitemap lastmod 2026-06-03.

PRIOR STATE (v3.63.126, retained for context until next release reshuffles the margin):
Mirrored the v3.63.125 reframe on ai-cover-letter-editor.html. Same insight: the application portal screens cover letters via ATS, recruiter copilot tags fit-signals against the job description, hiring manager (if they read at all) sees a one-paragraph AI summary before the actual letter. So if AI is the first reader, refine the cover letter using AI. Added matching "This is literally how WaxFrame started" .wf-card.is-green right after the trial CTA — cover-letter-flavored origin story (David's proto-WaxFrame workflow was used by hand on the cover letter that went with his 20-year-old résumé during the same job hunt, same multi-tab gauntlet, same four rounds, same result). Updated "Why a multi-AI hive beats one AI for cover letters" to reflect the three-layer screening reality (ATS keyword-match → copilot fit-signals → human scan). Meta description, OG, Twitter, JSON-LD all reframed. Same chassis, same body sections, same cross-links — just a stronger top.

PRIOR STATE (v3.63.125, retained for context until next release reshuffles the margin):
Reframed ai-resume-review.html lead. v3.63.123's hero buried the actual reason this matters: ATS systems screen résumés before any human sees them, recruiter copilots summarize them, hiring manager reads the pre-digested version. So if AI is the first reader, refine your résumé using AI. Added "This is literally how WaxFrame started" section (is-green wf-card) right after the trial CTA — David built the proto-WaxFrame workflow by hand on his own 20-year-old résumé using ChatGPT + Claude + Gemini in separate tabs, manually collected and reconciled feedback round by round, that's the résumé that got him hired. Origin story is the page's strongest credibility move. Also updated "Why a multi-AI hive beats one AI for résumé review" to reflect modern screening reality (ATS keyword-match first → recruiter copilot summary second → human scan third, each with different failure modes). Meta description, OG, Twitter, JSON-LD all reframed around the new lead. Same chassis, same body sections after the lead, same cross-links — just a stronger top.

PRIOR STATE (v3.63.124, retained for context until next release reshuffles the margin):
Tools section in the hamburger menu sorted alphabetically — Save as Template had been sitting at the top because of source order; now Backup Session, Backup Session (Scrubbed), Import Backup, License Key, Round History, Save as Template. Round History row swapped from inline SVG (oversized icon, visible gap to label) to ⏱ emoji to match the About modal at index.html line 2255. Pure presentation, no behavior change.

PRIOR STATE (v3.63.123, retained for context until next release reshuffles the margin):
Three new use-case SEO landing pages live: ai-cover-letter-editor.html, ai-business-proposal.html, ai-resume-review.html — each targets a defined search intent ("AI cover letter editor", "AI business proposal writer", "AI résumé review"), follows the linear helper-page chassis (no doc-layout, helper-body class, hero hp-section at top of .page-main, back-to-top, full SEO meta + JSON-LD Article schema), and funnels into the matching Template + playbook + AI API Pricing page. Cross-links added in both directions: each of the three matching tpl-cards on templates.html renders a "Read the … guide →" button (new .tpl-card-landing-link rule in style.css; LANDING_PAGES map in templates.html renderTemplateCard), and the three matching document-playbooks.html playbook descriptions get "For the short version of this workflow, see the AI X guide ↗" appended. sitemap.xml updated with the 3 new URLs (priority 0.7). David explicitly declined the RFP-response landing page ("too challenging") — only these three shipped. Mobile-tested live at 390x844 in Chrome MCP before push on all 3 pages: no horizontal page overflow, chrome 0-126, hero starts y=168, H1 at y=319, 4 theme buttons render, back-to-top resolves to #top.

PRIOR STATE (v3.63.122, retained for context until next release reshuffles the margin):
Trailing fix on v3.63.121. ai-api-pricing.html's H1 hero moved OUTSIDE doc-layout to match the pattern v3.63.121 just established on api-details.html. Now every doc-layout helper page (ai-api-pricing, api-details, document-playbooks, waxframe-user-manual, templates) has identical mobile flow: chrome → hero with H1 → menu → content. Consistent across the site. Verified at 390x844 in Chrome MCP: chrome 0-126, hero 168-541 (H1 at y=150), menu 572-1029, first content card #how-to-read at y=1078. Card-view pricing rows still render, 4 theme buttons visible, back-to-top works, no horizontal page overflow. Desktop unchanged.

PRIOR STATE (v3.63.121, retained for context until next release reshuffles the margin):
Comprehensive coordinated mobile fix after a real audit at iPhone 14 viewport (390x844). Three earlier releases (v3.63.118 / .119 / .120) shipped guesswork CSS without rendering the pages; v3.63.121 was tested in a local Chrome MCP browser against every helper page before commit. SIX fixes shipped together. (1) ROOT CAUSE: `html, body { height: 100%; overflow: hidden }` at style.css line 256 — the in-app rule that locks setup screens to viewport — was being applied globally, including to helper pages. `document.documentElement.scrollHeight === 844` on a 13000+ tall page, which broke back-to-top links, clipped long-form content, and was the actual source of David's "blowout" complaints on what-are-tokens.html. Scoped override via `html:has(body.helper-body), body.helper-body { height: auto; overflow: visible }` plus matching unlock on `.page-main`. In-app `index.html` keeps the viewport lock (no `helper-body` class). Verified live: clicking wh-back-top on privacy.html scrolls 3000→36. (2) THEME BUTTONS restored on mobile in a SECOND BAND below brand row via `flex-wrap` on `.page-header` — the first cram-them-in attempt looked wonky, the band version is clean. Header is 126px tall on mobile (was 72px) — +54px for the theme band, worth it. (3) `id="top"` anchor + back-to-top link added to api-details, what-are-tokens, prompt-editor, privacy, terms, help (6 pages had neither). (4) api-details.html restructured to match document-playbooks pattern: hero hp-section (with H1 "Get Your API Keys" + bee + intro) moved OUTSIDE doc-layout. Now: chrome → hero with H1 → menu → API key cards → Know Your Hive → General Tips. No more "split between two documents" feel. (5) Pricing card-view scoped to `.ai-table.is-stack` modifier instead of all `.ai-table`. v3.63.118's blanket selector broke what-are-tokens.html's "Best Builder Choices" table (no data-label attrs); now that table uses the `:not(.is-stack)` horizontal-scroll fallback. (6) v3.63.118/119/120 doc-layout `order` / `display:contents` experiments fully reverted — source order is the right answer now that the overflow lock is gone. CSS comment captures why each clever variation failed so future eyes don't repeat the mistakes. .gitignore gains `.claude/` to keep worktree dev tooling out of the repo. Pricing arc state still holds (card-view on `.is-stack`; dynamic data via Worker + KV; sortable columns; PT/ET local-time stamp). SEO arc state still holds. Sitemap lastmod 2026-06-03.

Open Bugs

- importSession (Restore from Backup) writes LS_HIVE verbatim — does NOT normalize legacy IDs or stale display names. ROOT CAUSE: `storage.js:1416` does `localStorage.setItem(LS_HIVE, data.LS_HIVE)` for non-scrubbed imports, dumping the backup blob into localStorage with no transformation pass. Consequence: any backup file containing legacy ugly IDs (e.g. `cohere_1779690241686`, `together_ai_1779690170122`, `ai21__jamba__1780256643064` from pre-clean-id code, OR `cohere(2)`-style suffixes from the pre-v3.63.95 skeleton-collision bug) survives Restore intact, regardless of how many WaxFrame versions have shipped since. Also persists obsolete display names like "AI21 (Jamba)" from older QUICK_ADD_PROVIDERS presets. Confirmed reproducing 2026-05-31 on v3.63.95: David wiped localStorage, restored from a backup, and immediately had the same byte-identical timestamp-suffixed IDs back in his hive. Discovered while debugging David's "I wiped and re-added but they're still ugly" report — wipe + reload works correctly; Restore is the path bringing them back. NO STANDALONE WORKAROUND IN THE REPO. A `tools/scrub-waxframe-backups.mjs` script was discussed 2026-05-31 as an offline cleanup path, but it never landed in version control — `tools/` on `main` (verified 2026-06-01) contains only the screenshot-capture pipeline files. The standalone path is now considered obsolete: stacking the in-app `normalizeHive()` fix below with the selective-import diff-and-pick flow (under Open Features) cleans legacy backups on the way in, without needing an offline tool. Each legacy backup the user imports gets normalized + section-picked, producing a clean final state. PROPER IN-APP FIX: importSession should run a normalization pass on the imported hive before writing it to localStorage. Steps: (1) parse `data.LS_HIVE`; (2) for each customAIs[] entry, look up its endpoint in QUICK_ADD_PROVIDERS preset table and compute the clean id+name from the preset; (3) build oldId→newId rename map; (4) handle collisions with (N) suffix; (5) apply rename to all cross-references inside the parsed hive (same set the standalone script handles); (6) re-stringify and write. Same logic shape as the standalone script — could be ported into storage.js as a `normalizeHive()` helper called from importSession. Question to resolve at implementation time: should normalization fire silently, or surface a "Cleaned up N legacy IDs from this backup" toast so the user knows it happened. Lean toward the toast for transparency. REFERENCE IMPLEMENTATION: `cleaner.html` (offline tool, v1.2 build 20260601-skeleton-collision-fix) is a working version of `normalizeHive()` logic — validated against 68 real backup files from David's pile, 64/68 cleaned correctly, 0 collision-suffix damage. Port the JS functions `deriveCleanFromUrl`, `deriveCleanFromId`, `isUgly`, `idTaken`, `normalizeBackup` from cleaner.html into a storage.js helper. CRITICAL IMPLEMENTATION NOTE FROM CLEANER v1.2 BUGFIX: the `idTaken` collision check MUST exclude the `models` map — that map carries skeleton-only entries from api.js's pre-registration (cohere, together, deepseek), and counting them as real claims falsely triggers `(N)` suffix on legitimate renames. Mirrors the v3.63.95 in-app fix in `makeCleanProviderId.taken()`. Real claims live in `activeAIIds`, `keys`, `customAIs`, and `customAIConfigs` only. SECOND IMPLEMENTATION NOTE: the cleaner's `deriveCleanFromId` recognizes three patterns (`prefix_<13-digit-ts>`, `prefix__name__<13-digit-ts>`, `name(N)`) — but the 2026-06-02 audit of David's 68-file pile surfaced a fourth pattern from older Anduril/Alfredo work-gateway backups: `_base__<provider>_<model_variant>_<13-digit-ts>_<index>` (e.g. `_base__claude_3_7_sonnet_1776393640151_0`). These pass the `__`-based isUgly check but the cleaner correctly skipped them because (a) no URL match against the public-provider table, (b) no clean rename target — multiple Alfredo models behind one URL would collide if stripped to bare provider names. For the in-app port: surface unrecognized-pattern ugly IDs to the user via a "Leave as-is or pick a clean name" prompt rather than guessing. Don't invent rename rules for custom-gateway entries the cleaner can't resolve unambiguously.

Open Features

- Helper-page body-copy linking continuation — finish the v3.63.108 batch. v3.63.108 inserted "Apply it directly in WaxFrame from the X template ↗" body-prose links into 5 of 22 document-playbooks.html playbooks (cover-letter, resume, business-proposal, rfp, product-review — picked for SEO leverage). Mechanical pattern is now established and the per-template anchors exist on templates.html (id="tpl-${t.id}" stamped by renderTemplateCard). Remaining 17 playbooks: quick-start, job-description, linkedin-about, thank-you, email-campaign, executive-summary, blog-post, linkedin-post, presentation, recipe, contractor-letter, restaurant-review, hotel-review, business-review, trim-to-tripadvisor, trim-to-google-maps, rewrite-as-yelp. Each gets one Edit to its `<div class="dp-playbook-desc">…</div>` block: append `<a href="templates.html#tpl-<id>" target="_blank" rel="noopener noreferrer" class="link-accent"><Template Name> template ↗</a>` after the existing description sentence. Playbook id = template id 1:1 (verified). Low risk, ~17 mechanical edits + helper-page stamp sweep. Defer to a release that has another reason to touch document-playbooks.html so cache-busts amortize.

- index.html multiple-H1 audit — partial work shipped in v3.63.104 (9 H1s → 3 H1s by demoting 6 setup-screen titles to H2s). Item now closed unless further consolidation needed. Spot-check after a few weeks of search-console signals before re-opening.

- Custom Templates Phase 2 + Phase 3 — full editors behind the scaffold buttons that landed in v3.63.97 on templates.html. The dedicated page exposes "Create Blank Template" and "Import Template File" callouts but both currently open a "Coming Soon" modal. PHASE 2 (Create Blank): guided multi-step editor that builds a custom template from scratch without requiring a finished project — fields are the same set the Save-as-Template flow already collects (name, icon, category, paths, per-path Project fields, optional length-mode, optional hive snapshot). Natural home is a sub-page or full-screen modal off templates.html. Could be implemented as a dedicated `template-editor.html` page (consistent with the dedicated-page direction) or as a large in-page modal — leaning sub-page for full-screen real estate and a stable URL for in-progress edits. PHASE 3 (Import .json): file-picker that reads an exported `.json` template, validates the shape (must have id, name, paths, pathContent; warn on missing optional fields), deduplicates against existing customs (rename if id collides), and writes to `localStorage.waxframe_custom_templates`. Reuse the `_safeImport*` hardening pattern already in storage.js for backup imports. Lower risk than Phase 2 — pairs naturally with the per-card Export that already works. PHASE 1B — hive auto-reconcile on apply — separate concern; mutates setup-flow state. When a custom template is applied and its captured hive includes AIs the user no longer has configured, today the apply silently drops them. Reconcile flow: pre-select keyed AIs that match, flag missing AIs explicitly, offer "use this template's hive (and let me add the missing AIs)" vs "keep my current hive (skip the template's AI lineup)". Riskier than 2/3 because it touches the live setup state, not just template storage.

- Templates page Phase 1 follow-ups (post-v3.63.97 ship) — small refinements that surfaced while building the dedicated page but didn't make this release: (a) search/filter bar on top of the library grid (search by name/description; filter by category; filter by Scratch/Refine paths) — skipped for v1 since 23 built-ins fit on screen comfortably, revisit when the library grows past ~40 entries; (b) sort options on My Templates — currently alpha; consider "recently saved" as a secondary option; (c) per-card "View Details" expander showing the full goalDocType / goalAudience / goalOutcome / refMaterial preview before applying (currently the card only shows the 1-line description); (d) per-card duplicate action on customs — clone a custom template as a starting point for a variant; (e) bulk Export All customs as a single ZIP — pairs with future bulk Import.

- Rename "Backup Builder" → "Failover Builder" (or "Secondary Builder") — vocabulary collision cleanup paired with the Backup→Checkpoint rename below. CURRENT: Settings exposes a `setAutoBackupBuilder` and a "Backup Builder" concept (index.html ~835) that is actually the auto-failover Builder fired when the primary Builder hits CONTENT_FILTERED / EMPTY_RESPONSE. The word "Backup" here means "failover redundancy," not "saved-state file" — but with the file-save flow being renamed Checkpoint, "Backup Builder" becomes the lone remaining user-facing "Backup" surface and reads ambiguously. PROPOSED: rename the user-facing label to "Failover Builder" (preferred — describes the behavior precisely: takes over when primary fails) or "Secondary Builder" (alternative — describes the relationship without committing to a failure trigger). Pairs with the in-progress "Builder Failover" feature entry below — that entry is the BEHAVIOR; this entry is the LABEL. INTERNAL CODE NAMES STAY: `setAutoBackupBuilder`, any related localStorage keys, internal handler names keep their existing identifiers — refactoring those is gratuitous churn and breaks every prior commit/changelog reference. Same scope discipline as the Checkpoint rename: copy/UX layer only. SURFACES TO HIT: Settings modal label + tooltip, any in-app copy that references "backup builder," README mentions if any, CHANGELOG terminology going forward. RISK SURFACE: zero — the label is internal-app-only, not in any user-saved data. Idea credit: noticed 2026-06-01 while scoping the Checkpoint rename — naturally surfaced once "Backup" was being audited as a term.

- Move Deep Dive toggle off the dev toolbar to a user-discoverable surface. CURRENT: Deep Dive (the WF_DEBUG ring buffer that captures forensic per-call telemetry — API request/response shapes, finishReason, payload sizes, timing, error codes) lives behind a 🔬 toggle on the dev toolbar (index.html ~2494). OFF by default, which is correct. The viewer modal (index.html ~1746) is well-built, but the toggle is invisible to anyone without the dev toolbar open — which is every non-David user. Consequence: troubleshooting a real-world support case requires the user to first know Deep Dive exists, then know how to find the dev toolbar, then enable it, then reproduce the issue. PROPOSED: dual-home the toggle. (1) help.html — at the top of the diagnostic-bundle workflow: "Want richer data in your next bug report? Turn on Deep Dive, reproduce the issue, then come back here to export." Click → toggle on → toast confirms. This is the discovery path — users only land on help.html when something's already wrong. (2) Settings → new "Diagnostics" section — quieter persistent surface, labeled "Capture detailed diagnostic info (slows things down slightly)" with help-text link explaining what gets captured. This is the persistence path — power users who want it always on. Dev-toolbar toggle stays for David's speed-of-access. NON-GOAL: Deep Dive is NOT a customer-facing feature; the Transcript export (`exportTranscript()` in app.js ~17684) is. They're orthogonal — Transcript captures CONTENT (the work product: every reviewer's actual writing, every Builder pass, every USER DECISION block; permanent record of "what did my hive collectively do"); Deep Dive captures MECHANICS (the technical guts: only useful when something's wrong). Neither is redundant with the other; killing either makes the other worse. Idea credit: scoping question 2026-06-01 — "what's the difference between Transcript and Deep Dive and why both?" — answered by reading the actual code paths.

- Rename "Backup / Restore" → "Checkpoint / Restore from Checkpoint" (vocabulary + framing change across the whole arc). CURRENT: every user-facing surface that touches the backup flow uses defensive IT language — "Backup", "Restore from Backup", "Backup file", "Send to email as backup". That framing positions the feature as recovery-from-disaster rather than as a deliberate save-point the user returns to on purpose. PROPOSED: shift the whole vocabulary to gaming-language "Checkpoint" — button labels, modal titles, menu items, in-app copy, filename stems (`Project-vX.Y.Z-WaxFrame-Checkpoint-YYYYMMDD-HHmm.json`), help-page references, README mentions. KEY USE-CASE FRAMING THIS UNLOCKS: "Save a checkpoint before applying decisions" / "Save a checkpoint before a hive composition change" / "Jump back to the checkpoint where the document was working" — each one is a deliberate save-point, not a panic move. Pairs natively with the selective EXPORT entry (which sections to include in this checkpoint) and the diff-and-pick IMPORT entry (which sections to bring back from a checkpoint). The selective-import diff modal becomes "Restore from Checkpoint" with clear section toggles — feels like cherry-picking from a save state, not a destructive recovery. SURFACES TO HIT: index.html footer-pill labels and tooltips, menu items, save-session/backup-session button copy, the trust-warning modal title, `backupSession` toast strings, helper-page references in `getting-started.html` / `user-manual.html` / `help.html`, README, CHANGELOG terminology going forward. INTERNAL CODE NAMES STAY: `backupSession`, `LS_SESSION`, `IDB_SESSION`, `_waxframe_backup_scrubbed` keep their existing function/blob names — refactoring identifiers is gratuitous churn and breaks every prior backlog/changelog reference. The rename is a copy/UX layer, not an internal-API layer. ANTICIPATED CONFIRMATION: the word "checkpoint" is already creeping into the code unsystematically — index.html:1927 carries `<!-- FIRST-RUN OFFER MODAL (v3.63.41) — merged checkpoint-backup + save-as-template -->` and index.html:1933 reads `💾 Save a checkpoint backup`. The rename CONSOLIDATES language the product is already groping toward, rather than introducing new vocabulary; lower risk, higher payoff. RISK SURFACE: existing backup files in the wild stay valid (format unchanged, only filename stem differs going forward) — older `*-WaxFrame-Backup-*.json` filenames still import correctly. Pairs with: selective EXPORT entry (below), diff-and-pick IMPORT entry (below), Backup Builder → Failover Builder rename (above).

- Selective backup EXPORT (producer-side counterpart to the diff-and-pick import flow below). CURRENT: backupSession at storage.js:959 captures the full local state — LS_HIVE, LS_PROJECT, LS_LICENSE, LS_SESSION, IDB_SESSION — into one JSON file with no choice over scope. PROPOSED: backup-generation modal lets the user check/uncheck what goes in the file: ☑ Project (goal, reference material, length settings) / ☑ Session in progress / ☐ Hive composition / ☐ API keys / ☐ Builder selection / ☑ License (default ON, untick when sharing with a different user). KEY DESIGN POINT — License is user-choosable but defaults ON: license is per-user not per-install, so for self-backups (work→home, machine→machine) the user wants license to ride along. BUT for coworker sharing (David's collaboration use case: hand a project to a teammate who has their own license and continues the work), the user needs to be able to strip it. Defaulting ON keeps the common case one-click; the checkbox covers the share case. RETIRES THE SCRUBBED-BACKUP SPECIAL CASE: today's `_waxframe_backup_scrubbed: true` flag and its receiver-side merge branch in importSession (storage.js lines 1398-1417) exist specifically to do this one thing — strip license + keys when sharing setups with different users. Once selective export ships, that special case is redundant: the user just unticks License + Keys at export time, accomplishing the same outcome through the general mechanism. Plan to delete the `_waxframe_backup_scrubbed` code path as part of this feature's implementation. KEY USE CASE — portable projects across machines: David starts a project at work using Anduril's Alfredo (internal AI gateway, only reachable from the work network), mails the backup to himself at home, wants to continue the project at home using his personal hive (different AIs, different keys). Today this is broken — restoring the work backup at home replaces home's hive with the work hive including the unreachable Alfredo entry. With selective export, at work the user unticks Hive + Keys + Builder (License stays ticked — same user on both ends). Mailed to home, imported via the diff-and-pick UI, the home hive is untouched and the project lands on top; license matches local so no change. SECOND USE CASE — coworker handoff: David mails the same project to a teammate. This time he ALSO unticks License (and probably Keys — teammate has their own). Teammate imports, gets the project + David's hive composition (so the same AIs participate going forward), but uses their own license and their own keys. ARCHITECTURAL SHAPE: the omitted sections write as `null` in the backup file (mirrors how `_waxframe_backup_scrubbed` backups already null out LS_LICENSE), so the format stays one envelope and the consumer's diff-and-pick UI naturally renders "Not included — your existing X will be used" for nulled sections. RELATED OPEN PROBLEM — session-AI mapping when a portable session lands on a hive missing some of its referenced AIs: `IDB_SESSION.sessionAIs[]` and `IDB_SESSION.ringBuffer[].ai/.provider` may reference AI IDs that don't exist in the destination hive (e.g. Alfredo at work, missing at home). Three reasonable behaviors: (1) drop unknown IDs from sessionAIs and ringBuffer, session continues with whatever local AIs remain; (2) drop with explicit user-facing warning "3 AIs from the original session aren't in your local hive: alfredo, [...]. Their past contributions are preserved in the document but they won't participate going forward"; (3) remap — let the user pick a local AI to fill in for each missing one, future-only, past contributions stay attributed to the original. Option 2 is the cleanest default.

- Restore-from-Backup diff-and-pick UI — turn the current blunt overwrite (storage.js importSession at line 1416) into a comparison + selective-import flow. CURRENT: user picks a backup file, gets a one-shot trust-warning modal, then the entire backup state replaces local state with no preview of what's about to change. PROPOSED: after the trust-warning, before any writes happen, show a comparison modal that diffs backup vs current local state across these categories: (1) Hive composition — which AIs are in backup but not local (checkbox: import?), which are in local but not backup (checkbox: keep?); (2) API keys — keys matching, keys in backup only (import?), keys differing between backup and local (pick which to keep, per-key); (3) Per-AI model selections — when backup has a different model for the same AI, surface the choice; (4) Builder — when backup's Builder differs from local's, surface the choice; (5) Project + session — show backup project name vs current, show backup session round-count vs current, let user choose: project only / project + session / neither; (6) License — choice surfaced when relevant: if backup contains no license (was unticked at export) → row not shown; if backup license matches local → "License: matches your current install" (info-only, no checkbox); if backup has license and local doesn't → "License: import from backup ☑" (default ON); if backup has DIFFERENT license than local → "Backup license belongs to a different user — keep your current ☑ / use backup's ☐" (default to keeping local; never silently overwrite a valid local license); (7) Legacy IDs — once the importSession-normalize fix ships, show the diff using POST-normalization IDs so the user previews the final state, not the raw backup IDs. User checks/unchecks what they actually want, hits Apply, and WaxFrame writes only the chosen pieces. HEADLINE USE CASE — portable projects across hives (David's work/home split): work backup contains Anduril's Alfredo as Builder, unreachable at home; user imports at home, diff-and-pick lets them tick "import project + session" but leave "hive composition" + "keys" + "builder" unticked — the project lands on top of the home hive without trying to add an unreachable AI; license auto-resolves to "matches your current" (same user on both ends). Pairs with selective-export above (the producer-side counterpart) to make the round-trip clean. OTHER SCENARIOS THIS SOLVES: "I was 4 rounds into a session and accidentally restored a backup that wiped it" — diff shows "will replace your 4-round session in progress" before committing; "I just want the keys from this backup, not the whole hive composition" — selective import handles this directly; "Backup was from when I had Perplexity but I removed it later" — diff shows "Perplexity will be added back to your hive" with explicit checkbox to skip; "Did I just bring back ugly IDs?" — diff shows the final post-normalization IDs before any writes. ARCHITECTURAL NOTE: storage.js already has ONE special-case merge path for `_waxframe_backup_scrubbed: true` backups (line 1398-1417) that does exactly this kind of selective merge — preserves receiver's keys/license while importing sender's hive composition. This feature generalizes that one-off special case into the always-on flow, eliminating the need for a separate scrubbed-backup code path. Pairs with: the importSession ID-normalization bug fix above (the diff would show normalized IDs as part of the preview); and with the selective-export entry above (round-trip portability).

- Mistral 429 proactive warning via X-RateLimit-Remaining header. Mistral's official docs note their API returns an `X-RateLimit-Remaining` response header. WaxFrame doesn't currently read it. Adding header capture to the Mistral provider path (and any other provider that exposes the same shape) would let the troubleshooting layer surface a soft warning BEFORE a 429 hits — e.g. a yellow chip on the Mistral row that reads "Mistral: ~X requests left this minute" when the remaining count drops below a threshold (e.g. < 3). The 429 itself stays catalogued the same way; this just adds a pre-warning surface. Pairs with the API console URL split (right destination for usage info matters here too). Likely needs a small per-provider hook in the response-handling path in `callAPI` (app.js ~11500) since the available rate-limit headers differ by provider — Anthropic exposes `anthropic-ratelimit-*` series, OpenAI exposes `x-ratelimit-*-requests` and `x-ratelimit-*-tokens`, etc.

- Hive parallel fan-out RPS-awareness. CURRENT: a reviewer round fires up to N concurrent calls (one per AI in hive), where N can be 10+. With paid Mistral on Tier 1 or 2 (low RPS), that parallel burst itself can trigger 429 even though the user has plenty of monthly token budget remaining. CONSIDER: an optional per-provider concurrency cap so a Mistral request gets queued one-at-a-time if the user's tier requires it. UI surface: a Settings toggle "Throttle Mistral to 1 RPS (paid tier compatibility)" — off by default, user enables it if they keep getting 429d. The implementation could be a tiny per-provider mutex/queue in `callAPI`. Workaround until shipped: user contacts Mistral support to raise tier (per official Mistral docs that's their stated path to higher limits — there's no automatic spend-based promotion ladder).

- Current API Pricing page (SEO arc #1 — highest leverage open SEO move). Generator script + static page surfacing live-ish per-model input/output token pricing for the providers WaxFrame supports: ChatGPT (OpenAI), Claude (Anthropic), Gemini (Google), Grok (xAI), Perplexity, Mistral, DeepSeek, Together AI, Cohere, Jamba (AI21). Driver: David picked Jamba on 2026-05-31 specifically because it was "cheap" and DeepSeek was slow — there's no in-app surface to compare cost vs speed at selection time, so users guess. Three viable approaches: (a) one-time hand-curated page with a "last updated YYYY-MM-DD" stamp, updated manually each release — lowest engineering cost, highest staleness risk; (b) a tools/ script that scrapes each provider's pricing/docs page nightly and emits a static `pricing.html` — moderate effort, fresh data, brittle on provider HTML changes; (c) per-provider pricing endpoint where available (very few providers expose this) plus manual fallback — most accurate, most maintenance. Recommend starting with (a) for immediate value, then graduate to (b) as a tools/ script that emits the same page format. Page columns to include: provider, model name, input $ per million tokens, output $ per million tokens, context window, max output tokens (matters for Builder-capability filtering — pairs with the Builder-capability entry below), per-call cost estimate for a typical WaxFrame review round (~5K prompt + 2K completion = ~7K tokens). Also include per-provider rate limits (RPS, RPM, TPM) and free-tier caps where applicable. Link from api-details.html so users hit it before picking a Builder or hive composition. NOTE: this entry consolidates the prior duplicate "Current API Pricing page" (Open Features) and "Pricing / cost comparison page" (Observations SEO arc) entries from v176 — same idea, different framings.

- Model recommender filter-awareness warning — when the recommender's pick has been flagged in the current session's ringBuffer with CONTENT_FILTERED, surface a ⚠️ next to that model in the Change Builder dropdown and in the recommend toast: e.g. "This model has triggered content filters this session — Flash variants are less aggressive". Doesn't block the pick. Self-learning from actual session history.

- Builder Failover — primary Builder hits CONTENT_FILTERED / EMPTY_RESPONSE → auto-failover to a configured Secondary Builder for that one round only, then snap back to primary next round. Mirrors how the hive already handles a single reviewer failing. Implementation: "Secondary Builder" pick on the Builder screen, interceptor in runBuilderOnly() that swaps to secondary on CONTENT_FILTERED, completes the round, resets. Lets users stay on cheap providers (Gemini Flash, DeepSeek) as primary with a reliable but pricier secondary (Claude/ChatGPT) ONLY firing on blocked rounds.

- Builder-capability filter at recommendation/selection time — flag models that can't fulfill the Builder role. Empirically confirmed broken Builders surfaced from production deep-dives so far: the entire `jamba-*` family (jamba-mini-2-2026-01 confirmed via 2026-05-31 BAGAIL session deep-dive with TWO distinct failure modes — outputs the refMaterial scaffold verbatim instead of synthesizing reviewer responses into polished prose, AND hits the 4096 max_tokens cap before reaching the %%CONFLICTS_START%% block; jamba-large-1.7-2025-07 separately confirmed by David in the same testing window — both Jamba models fail as Builder, so the whole family goes on the Builder-incapable list, Reviewer-only). Filter should run at: (1) Change Builder modal — greyed/warning-badged option with tooltip "May be unable to follow Builder formatting — try Reviewer role instead"; (2) Recommend Models pipeline — skip Builder-incapable models from Builder recommendations entirely. Mechanism shape: a `builderCapable: boolean` flag on the model metadata, populated from a known-bad list initially (start: all jamba-*), augmented over time by capture from ring-buffer evidence (finishReason === "length" on Builder calls + missing %%CONFLICTS_START%% pattern). PAIRED FIX — surface a clearer error than the generic BUILDER_NO_CONFLICTS_BLOCK when finishReason === "length": say something like "Builder output was truncated at the model's token cap — try a model with higher output capacity. This model hit max_tokens before completing the required formatting block." Same general shape as the existing "Recommend-time filter for instruction-incapable models (e.g. antigravity-preview)" parking-lot item — that's the same pattern at a different layer. UX cost of NOT shipping this: new user picks a cheap-but-incapable Builder, hits the failure, has no actionable diagnosis, gives up on the product. Real conversion risk.

- Legacy ID display cleanup migration helper (Tier 2 of the rename arc; Tier 1 — inline rename pencil in Manage AIs — shipped in v3.63.96). Legacy entries from older code carry timestamp-suffixed IDs (e.g. `ai21__jamba__1780256643064`, `cohere_1779690241686`) and obsolete display names (e.g. `"AI21 (Jamba)"` from the pre-rename QUICK_ADD_PROVIDERS preset). Tier 1 (now shipped) lets the user rename the display label without touching the ID — fixes the visible "AI21 (Jamba)" problem but leaves the ugly underlying ID intact. Tier 2 — a one-time migration helper — would rewrite legacy IDs in aiList, API_CONFIGS, the keys map, the hive Set, customAIs, customAIConfigs, and any session/hive references (same logic shape as the standalone `tools/scrub-waxframe-backups.mjs` script, ported into the app). Higher risk than Tier 1; defer until needed. PAIRS WITH the open `importSession` ID-normalization bug above — both touch the same `normalizeHive()` helper if it lands.

- Amazon Product Review canonical length recipe (reference data for templates.js) —

  Amazon length recipe (canonical):
    Minimum:  500 chars
    Normal:   1,500–3,500 chars (sweet spot)
    Detailed: 4,000–7,500 chars (long-form, thorough)
    Hard cap: 20,000 chars (Amazon's actual ceiling; 4K is recommended)

  Other platform caps for reference:
    Best Buy: 5,000 chars (hard)
    Target / Walmart: 4,000 chars (hard)
    Newegg: 5,000 chars (hard)
    Manufacturer site / blog: no limit
  Equivalent recipes TBD; gather from live runs.

- Product Review per-platform templates — extend the per-platform template pattern (established in v3.63.94 with `product-review-amazon`: 100-char title cap + Amazon body length recipe Min/Normal/Detailed/HardCap baked into the template). Pending platforms with known constraints: Best Buy (~5,000 char body cap; verify title cap), Walmart (~4,000 char body cap; verify title cap), Target (~4,000 char body cap; verify title cap), Newegg (~5,000 char body cap; verify title cap), Yelp (separate template already exists in the "Manly Bikes Yelp" lineage — confirm coverage). Each platform gets its own template, not an overlay on a generic one — the overlay approach was tried and failed in practice (v3.63.92 generic Product Review run produced a 134-char headline on a 100-char Amazon target). The generic `product-review` template stays as the no-platform default for blogs, manufacturer sites, and undecided cases; it carries a soft 100-char headline ceiling as a safe upper bound.

- GitHub Actions release-ceremony consistency check — workflow under .github/workflows/ that runs `node --check` on all JS, verifies the four version stamps match across files (meta waxframe-build / APP_VERSION / BUILD / app.js?v=), and validates no dead local asset links, on every push. Automates the manual release verification.

- Pause/resume at reviewer seam — finer-grained pause point INSIDE a round, between reviewers finishing and the builder firing. Current pause/halt mechanics all operate at round boundaries. The reviewer-seam pause would let the user inspect reviewer outputs, edit notes, and decide whether to run the Builder for this round — without having to wait for the Builder to finish + then halt.

- File System Access API exploration — investigate replacing download-only backups with true file persistence via window.showSaveFilePicker / showOpenFilePicker / FileSystemHandle. Would let a user "Save backup" once and then "Save" repeatedly to the same file without the download-folder pile-up. Browser-support check (Chrome/Edge yes, Safari/Firefox no) gates whether this is additive or a hard cutover.

- Provider Catalog + Hive Profiles Foundation — introduce a two-layer provider model using API_CONFIGS as the verified provider catalog and the user hive Set as the active selection. Subsumes the prior "Hive Profiles", "Two-layer AI management", and "Sources view" backlog entries — they're the same arc, scoped together. ALSO COLLAPSES THE "DEFAULT SIX" CONCEPT: the privileged-class distinction between "default" AIs and "custom" AIs goes away as a code-level abstraction. The current `knownDefaultIds` array (used to distinguish defaults from customs in handlers, validation routing, work-screen card sorting, and several other places) gets replaced by a `starter: true` flag on the catalog entries themselves. No first-run UX regression — same six AIs land on Worker Bees on first run, marketing claim "six top-tier AIs out of the box" stays true, but internally there's no special "default" branching, no separate arrays, no "default vs custom" gates. Question David surfaced 2026-06-01 ("do we really need a default six anymore?") is answered by: keep the curated starter set as a default Hive Profile preset, drop the privileged code class. Both are true simultaneously.

  Sequencing (most-conservative first; don't skip steps):
    (1) Add `starter: true` flag directly to API_CONFIGS entries — current six get the flag, no UI change. First-run Worker Bees screen filters to `starter:true` entries plus anything the user has already activated. Establishes catalog/hive distinction without visible disruption. Migration-risk phase: renderWorkerBees(), _selectedCustomIds, saveSession shape, backup import path (older backups lack catalog metadata — handle gracefully), license-bound round counting if hive size affects entitlement. If phase 1 lands clean, phases 2-4 are additive.
    (2) Build "+ Add from Catalog" modal on Worker Bees — grouped sections: Cloud API / Local Server / Router-Aggregator. Each entry shows icon + description + auth requirements. Activating a catalog entry flips it into the visible hive. Surface light "two AIs is valid, four or more is usually stronger, three can be less stable depending on the mix" copy — permission to start small without overexplaining the convergence data.
    (3) Catalog expansion in two batches (different setup/failure stories — don't mix into one PR unless plumbing is already very clean):
        Batch 1 — Cloud API: Perplexity, Cohere, Together AI
        Batch 2 — Local Server: LM Studio, Ollama, Alfredo, Open WebUI
    (4) Hive Profiles as saved snapshots of { activeProviders: Set, perProviderModelChoice: Map, roles: Map }. Presets to ship: Starter (current 6), Cheap 2-AI, Cheap 4-AI, Power (Starter + Perplexity + Cohere + Together), Local-only (LM Studio + Ollama + Alfredo + Open WebUI), Custom (user-named). Default load on first run = Starter.

  Catalog entry schema must carry `kind`: "cloud" (needs API key) | "local-server" (needs reachable base URL, model discovery, maybe no key) | "router-aggregator" (proxies to many models). Not taxonomy theater — it changes setup flow, testing approach, failure modes, support copy, and onboarding question ("have a key?" vs "have a server running?").

  Hard rule for catalog inclusion: "known AND verified end-to-end in WaxFrame's workflow," not "exists in the world." Each new catalog entry requires: chat-completion endpoint validated against real auth (NOT just /v1/models which is often public), conflict-extraction parser tested on actual responses, model-list endpoint working, per-provider model picker behavior verified, and for local-server: CORS/reachability discovery + reasonable error messaging when the server is down. Aspirational entries (e.g. OpenRouter as a name we recognize without having validated all of the above) poison the catalog's trust signal. Ship 7 that all work, not 14 with two flaky ones.

  Custom AI unchanged. Always-available escape hatch for genuinely unknown / OpenAI-compatible / hand-rolled endpoints. NOT subsumed by catalog — catalog is "we know this provider," Custom AI is "we don't, here's the raw config."

- CSS dead-selector cleanup branch — ~155 flagged selectors, some confirmed live. Static grep insufficient: display:contents is load-bearing; JS template-literal class names evade grep. Dedicated branch, phased deletion, visual smoke test on every screen as pre-deploy gate. Flagged clusters: welcome-card, save-bar, goal-modal-refine-*, ai-bfb-*, autosave/verify-panel remnants.

- CSS rule consolidation sweep — Codex read-only audit (post-v3.63.92) flagged 6 cases of same-component-language-multiple-custom-classes worth consolidating. Stats: style.css has 2,330 parsed CSS rules, 221 declaration sets duplicated somewhere; most repeats are harmless but these are real consolidation targets:
    (1) .btn-danger (style.css ~7685) + .license-modal-btn-danger (style.css ~7861): same red outline/fill/glow idea. Keep modal-specific sizing/layout on .license-modal-btn-danger, let .btn-danger own danger colors.
    (2) #wfConfirmOkBtn.wf-confirm-danger (style.css ~7030) + .length-guard-continue (style.css ~7154): identical soft-red base + hover colors. Candidate: new .btn-soft-danger.
    (3) .length-guard-indicator (style.css ~7351) + .slow-responder-indicator (style.css ~7419): nearly identical 15-property footer pill shells. Candidate: new .footer-status-pill.
    (4) .work-line-numbers (style.css ~2311) + .proj-ta-nums (style.css ~5252): exact same 20-property gutter styles. Candidate: new .line-number-gutter.
    (5) .custom-ai-modal-overlay (style.css ~3198) + .license-modal-overlay (style.css ~7727): same overlay shell. Candidate: new .modal-overlay.
    (6) .btn-accent:hover (style.css ~359) + .footer-btn-smoke:hover (style.css ~2863): same hover flip/glow.
  Intentional exception that stays as-is: help.html inline .btn-danger copy (help.html ~116) — break-glass page, no external CSS dependency, divergence is documented in both the help.html and style.css comments.
  Approach: introduce shared base classes, migrate callsites one cluster at a time, keep original classes only when they carry unique extras beyond the shared pattern. Risk surface is visual regression — full screen-by-screen smoke test (every modal, every screen, both themes) as pre-deploy gate, same discipline as the dead-selector cleanup branch.

- Repo housekeeping — `docs/` folder in the repo holds older backlog versions, prior playbooks, audit files, prompts reference, getting-started/readme `.docx` variants. ZIP ships latest backlog + Rules Reference + 3-version rollback margin only. Trim repo `docs/` to current margin OR confirmed intentional retention. Note: web-chat session 2026-06-03 already trimmed several stale items (older versioned PDFs/DOCX, v170-v172 backlogs, old audit findings, old User_Manual.txt). Pending uncommitted on `main` — verify clean after the v3.63.108 commit lands.

- Triage CodeQL + Dependabot findings once first scans complete. Vanilla innerHTML/template-literal patterns will likely flag — some real, some false positives on audited patterns. Walk the list together. Blocked on David pulling Security tab findings.

Observations

- v3.63.108 shipped 2026-06-03. Three of Codex's six SEO-arc ideas now have ground-truth signals: Schema expansion (FAQPage + HowTo, this release), Body-copy internal linking continuation (partial — 5/22 playbook → template anchor links + api-details ↔ what-are-tokens prose cross-links). Pricing page (Codex #1) and Use-case landing pages (Codex's strongest pick) remain unbuilt. Updated priority ordering after this ship: (1) Pricing page still the highest-leverage single-page move; (2) Finish the document-playbooks → template body-link sweep (mechanical 17-edit follow-up captured in Open Features above); (3) 2-3 use-case landing pages — test the pattern; (4) Template detail pages — defer until #3 proves out; (5) Comparison pages — skip unless real angle.

- cleaner.html (offline backup-cleaner tool) exists as a working reference implementation for the eventual in-app `normalizeHive()` port. Version v1.2, build 20260601-skeleton-collision-fix, ~830 lines single self-contained HTML, vanilla JS, zero external dependencies, air-gap safe. Lives in David's local working tree (Dropbox), NOT committed to the repo (no production value; dev/migration use only). The cleaner exposes the same logic shape the in-app fix needs: `deriveCleanFromUrl` (matches endpoint URL against the public-provider table), `deriveCleanFromId` (fallback ID-pattern stripping for three known shapes), `isUgly` (legacy-pattern detector), `idTaken` (collision check excluding the `models` map), `normalizeBackup` (full pipeline applying renames across all 8 cross-reference locations: activeAIIds, builder, keys keys, models keys, customAIs id+provider+name, customAIConfigs keys + label, IDB_SESSION.sessionAIs, IDB_SESSION.ringBuffer ai/provider/aiId). All logic transplants cleanly into storage.js as helper functions called from importSession. UI surface (drag-drop, FSA folder save, ZIP fallback) is offline-tool-specific and does NOT transplant.

- 2026-06-02 cleaner audit results against David's full 68-file backup pile: 68/68 parsed without error; 64/68 cleaned to fully clean state (zero legacy ugly IDs, zero collision suffixes); 0/68 had any `(2)`-style false-collision damage (v1.2 fix confirmed working at scale); 4/68 outliers all contained Anduril/Alfredo work-gateway custom AIs (`oui.ai.usgw1.aws.anduril.dev`) with the `_base__<provider>_<model>_<ts>_<index>` ID pattern — correctly skipped by the cleaner since no rename target could be unambiguously derived. The audit serves as a proof-of-correctness reference for the in-app port and as a real-world test fixture for the upcoming `normalizeHive()` implementation.

- Adoption-friction note (session context): David surfaced "zero sales after months of build" anxiety 2026-06-01. Process observation: at session time, Reddit r/SideProject post pending, HN Show HN pending community participation, trademark filing deferred, waxframe.com live but not referenced anywhere with reach, YouTube demo exists but unpromoted. Architectural decisions (e.g. "do we need a Default Six?") should NOT be re-litigated on sales speculation — the engineering merit answers stand independent of conversion data. The funnel has not been opened yet; conversion is untested, not failed.
