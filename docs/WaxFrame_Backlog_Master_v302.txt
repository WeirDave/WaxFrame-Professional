WaxFrame Backlog Master v302
2026-09-20

ABOUT THIS DOCUMENT
────────────────────────────────────────────────────────────
This is the canonical list of open work for WaxFrame
Professional — bugs that need fixing and features that need
building. Each entry is written to stand on its own so any
person or AI assistant can pick it up cold and understand:

  WHAT     — what the thing is, in plain language
  WHY      — what problem it solves or what value it adds
  STATE    — current status (OPEN / PARTIAL / PARKED / DEFERRED)
  WHERE    — file:line pointers into the codebase
  DONE     — what "done" looks like (acceptance criteria)

Companion document: docs/WaxFrame_Playbook_Test_Master_vN.txt
(test runs + measured convergence data).

Changelog content does NOT belong here. Released work goes
to CHANGELOG.md and is removed from this file the moment it
ships.






v302 — SECURITY SURFACES CLOSED OUT (v3.63.531). Open list unchanged: the one
PDF item, which needs David.

The three areas v3.63.530 explicitly did NOT claim are now covered.

1. STORAGE IMPORT SANITISER. consoleHTML is the only field in a checkpoint
   that is deliberately re-rendered as markup (storage.js:794). Tested live
   against img-onerror, javascript: href, inline on* attribute, svg onload and
   iframe: none survived, window.__wfPwned never set. Critically the check also
   asserts a LEGITIMATE console entry survives — a sanitiser that nukes all
   input passes every safety assertion while breaking the feature, and the
   first version of this test failed exactly that way (it was reading
   body.innerText, which skips the hidden work-screen console).

2. DEPENDENCY ADVISORIES. Checked every vendored lib against the GitHub
   advisory database. Clear: docx 9.7.1, jszip 3.10.1, mammoth 1.13.1, sheetjs
   0.20.3, pdfjs-dist-esm 6.3.289. NOT clear and cannot be: pdfjs-dist-classic
   3.11.174 is below the fix for GHSA-wgrm-67xf-hhpq (CVE-2024-4367) and is
   pinned there permanently — see v299's note. Mitigated by isEvalSupported:
   false, which the gate now asserts at every getDocument() call site and which
   was mutation-tested.
   NOTE WORTH KEEPING: GHSA-hq66-cqwq-w95j (HIGH) covers pdfjs >= 5.6.83 and
   < 6.2.108. v3.63.528 landed on 6.3.289 and is past it, but 6.0.227, 6.1.200
   and 6.2.x-before-.108 are all INSIDE it. A floor now enforces that.

3. CLOUDFLARE RELAY. Reviewed: origin allowlist, path allowlist, method
   allowlist, key required and never stored, rate limit on a hashed key, size
   guard checking BOTH declared Content-Length and actual byteLength, no
   Access-Control-Allow-Credentials. No findings. One undocumented property
   written up: absent-Origin requests are allowed (needed for file://), so the
   relay is reachable by non-browser clients bringing their own key.

STILL NOT CLAIMED: this is not a general security audit. No penetration test,
no review of the AI providers themselves, no threat model for a hostile
provider beyond output escaping.

v301 — HTML-SINK AUDIT DONE (v3.63.530). One open item, and it needs David.

The claim "all the code has been examined against security stuff" was NOT true
before this release and is now true for one specific class: HTML injection into
the UI. It is not a general security audit and nobody should describe it as one.

Result: 126 HTML sinks in js/, 103 static or fully escaped. Reviewer suggestion
text, AI display names and server-supplied model names are all escaped at their
render sites. A runtime check seeded hostile markup into AI names, project name
and reference titles, walked every screen that renders them, and found zero
elements created — the text arrived escaped.

Method note worth keeping, because two attempts failed first: static analysis of
this codebase does not work for this question. A hand-rolled scanner cannot find
where a template literal ends, and both attempts reported consoleLog() and
toast() strings as sinks. The third approach — seed hostile data, boot the real
app, ask the DOM what became an element — is the one that answers it.

The committed check asserts LIVENESS before safety: it fails if the hostile
value never loaded or never reached the DOM. Its first version passed while
proving nothing, which is the failure mode to watch for in every check here.

Mitigation context: script-src has no 'unsafe-inline', so injected handlers do
not run. Escaping is the first line, CSP the second.

NOT covered and not claimed: dependency vulnerability review beyond Dependabot,
the Cloudflare relay beyond its own test, and the storage import sanitiser.

v300 — OPEN WEBUI VERIFIED, BACKLOG EMPTY (v3.63.529). Old #1 struck. There
are no open items.

The Open WebUI .ollama wrapping is confirmed against a live server, which is
what the entry actually asked for. limitsFromModelEntry('openai-models', entry)
run over the real /api/models response: 10 of 10 entries with a context figure
matched ollama.details.context_length exactly, 8192 through 262144. The 11th
(Open WebUI's virtual "Arena Model") carries no Ollama object and correctly
yields nothing.

Getting there needed two admin toggles that are OFF by default in Open WebUI
0.11 and are worth writing down, because the next person will hit the same
wall: auth.enable_api_keys and user.permissions.features.api_keys. With them
off, Account shows no API keys section at all and every model endpoint returns
401 while /api/config and /health stay open — which reads like a broken
container and is not one.

Found while verifying, and fixed in the same release: the Import from Model
Server error card printed the literal "{ai}" because it renders catalog text
without going through renderTroubleshootingCard, which was the only code that
substituted placeholders. Now one shared WF_DEBUG.substitute, and a test walks
the whole catalog asserting no placeholder survives.
STANDING PRACTICE: catalog
text is a template; any surface that displays it substitutes first.

v299 — PDF.JS UPGRADED (v3.63.528). Old #2 struck; one item remains.

Closed, but not the way the entry assumed. The ESM build went 4.10.38 ->
6.3.289 and verified clean. The file:// fallback CANNOT follow and this is
permanent: pdfjs-dist has published ESM only since 4.x, so no 4.x, 5.x or 6.x
release exists in a form a file:// page can load. 3.11.174 is the last major
with a classic build. Recorded in docs/vendored-dependencies.json so nobody
re-opens it as "the fallback is 200 releases behind" — it is pinned by a
browser rule, not by neglect. Getting off it would mean adding a build step,
which §3 forbids.

Found on the way, and the more useful finding: five URLs in the pdf.js load
path had no working cache-bust — two frozen ~90 releases, three with none.
Replacing lib/pdf.min.mjs did not change what a returning browser ran. Proved
live: the page kept reporting 4.10.38 after the file was replaced.

STANDING PRACTICE, because this will recur: a release sweep rewrites the stamp
it is replacing and nothing else, so a stamp that misses ONE release is skipped
by every release after it and freezes silently. The gate now checks ?v= stamps
in js/ as well as HTML, which is where the frozen pair lived.

tools/check-file-protocol.mjs is new: opens the real index.html from disk in
headless Chrome and asserts the portable path. Run it when touching the pdf.js
loader or the vendored builds. Not a gate stage — release-check is pure Node
stdlib by design and would start needing a browser.

v298 — DOCX VERSION IDENTIFIED (v3.63.527). Old #3 struck; items renumbered
1..2.

Closed properly rather than closed empty: the version is 9.7.1, established by
downloading every docx release in the window and hashing its published browser
bundle. The whole difference from ours is the wrapper — three lines at the top,
one at the bottom, and 33,170 identical body lines.

Two things to keep. The bundle contains "0.132.0", which is @oxc-project/runtime
and not docx; a version string inside a bundle usually belongs to whatever built
it. And lib/docx.min.js is locally modified — the UMD wrapper was swapped for an
IIFE — so its recorded hash will never match an upstream artifact. That is now
written in the inventory, because otherwise it reads as corruption. On upgrade,
take the published dist/index.umd.cjs and re-apply the same swap.

v297 — HISTORY REWRITTEN AGAIN (v3.63.526). Open list unchanged at 1..3.

The v3.63.525 sweep cleaned the working tree; this cleaned history. Recorded
because two things learned here will matter the next time.

First: --replace-text does not touch commit messages. It needs --replace-message
as well, with the same rules file. A pass that used only the first reported
success while a document name sat in a commit body.

Second, and worse: a verification script reported a string absent when it was
plainly in the file. It read every blob including binaries, so a batch holding a
large image overflowed the child-process buffer, the spawn failed, and the loop
did "continue" — dropping four hundred objects and calling the rest clean. A
verification that can silently pass is worse than none, because it is the only
thing standing between a force-push and a leak. Any future scanner treats a
failed batch as a hard error and asserts it read as many objects as it listed.

Also worth keeping: a replacement rule is a substring match. A rule written to
rewrite "at David's work" also rewrote the middle of "that David's workflow" in
398 blobs. Always diff the rewritten tip against the live tip before pushing —
that diff is what exposed it.

v296 — CONFIDENTIALITY SWEEP (v3.63.525). No item opened or closed; the open
list is unchanged at 1..3.

Recorded here because it changes what a future pass should look for. Searching
for the employer's name and the gateway's name — which is what the 2026-09-15
pass did, and it found them — cannot find a sentence that names neither and
still describes the author's own workplace. Six of those survived that pass and
stayed published for months: one in a source comment, three in release notes,
one on a live documentation page, one in this folder.

The live one was the worst of them and had nothing to do with code. A playbook
uses real reference material as its worked example, and one line of it listed
the identity software in use at a real workplace. It was on waxframe.com.

Standing practice, which is the part worth keeping: search for the SHAPE of a
sentence about a workplace, not for the names of things in it. The gate now
carries a rule for that, matching author-attributed phrasing only, so that
second-person copy addressed to the reader about the reader's own workplace
keeps working. Rules Reference's ENVIRONMENT section was rewritten in the same
pass and no longer describes a specific employment situation.

v295 — AUDIT ITEM CLOSED (v3.63.524). Old #3 struck; items renumbered 1..3.
The orphan/eval audit was re-run and everything it found was fixed in the same
release rather than recorded here, so the entry closes empty.

It is now a committed tool — tools/audit-dead-code.mjs — and not a procedure
somebody re-derives from a methodology doc. That is the actual finding. The
previous audit ran 2026-07-25, the next ran 2026-09-20, and in between five
genuinely dead things accumulated in code that nobody could check in one
command. The doc describes what to look for; the tool is what gets run.

The audit gained a pass it did not have before: dangling DOM lookups. Plain
reference counting cannot see a function that is called constantly and does
nothing, which is how two Builder renderers, a pair of round-timer writes and
a diff-mode button lookup all stayed in the file after the elements they wrote
to were deleted from index.html. Every one of them had live callers.

Two more only surfaced once the tool learned to ignore comments: a tier
classifier with no caller, and the singular getVisionCapableAI() that
v3.63.426 replaced — each was "referenced" solely by comments naming it.

v294 — FLOW HARNESS SHIPPED (v3.63.522). Old #3 struck; items
renumbered 1..4. tools/flow-check.mjs runs headless against a same-origin mock
provider: 23 assertions over the setup screens, two complete hive rounds, the
Change Builder pill and a checkpoint round trip, for zero API spend. Wired into
the CI job that already has Chrome — NOT into release-check.mjs, which is pure
Node stdlib by design and would start requiring a browser.

It earned its keep twice before it was even committed:

  • A mutation test re-introduced the v3.63.405 bug (setBuilder no longer
    refreshing the bee grid) and the harness caught it — pill stayed on the old
    card while state said otherwise. A test that has never failed proves
    nothing, so this was run deliberately rather than assumed.
  • Building it surfaced dead code: updateBuilderRequirements() wrote to
    the req-builder and builderContinueBtn elements, neither of which has
    existed since the standalone Builder screen was retired in v3.63.147.
    Removed. The first
    removal attempt broke navigation and the harness caught THAT too, within a
    minute of being written.


v293 — ONE SYSTEM OF RECORD (2026-09-20). David, on finding that an
empty backlog did not mean an empty queue: "I don't understand how we can have
more stuff hidden in different spaces, that seems ridiculous. We need to do a
better job of cleaning that up and being more fluid and transparent so that I
can understand exactly what the hell is going on."

He was right. CLAUDE.md §8 had been carrying real code items while this file
carried the rest, so "the backlog is empty" was true and misleading at the same
time. Those items are now entries HERE, written to the same five-field shape as
everything else, and §8 is a pointer with nothing of its own.

RULE, going forward: if it is work, it goes in this file. CLAUDE.md holds
process, standing orders and verified state — never a task list. A second place
to look is how items get lost.


v292 — QUEUE EMPTY (2026-09-20). The last numbered item — the refs the
v3.63.515 rewrite could not reach — was closed as ACCEPTED RESIDUE on
David's decision rather than escalated. The reasoning lives in the private
process notes and is deliberately not restated here: this file is public,
and the entry as previously written was a map of where to look.

There are now NO numbered items. **That is the finished state, not a gap.**
Do not invent entries to keep the count above zero. The habit this document
kept failing at was the opposite one — carrying items that were already done
— so an empty queue is the point of all that housekeeping, not a problem
with it.

Also fixed here: v287..v291 had left the v290..v283 notes stranded below the
OPEN FEATURES section instead of with the other notes. Section order is back
to ABOUT / history / OPEN BUGS / OPEN FEATURES / STANDING PRACTICE /
OUTREACH / NOTES ON FORMAT.


v291 — HISTORY REWRITE DONE (2026-09-20, v3.63.515). Old #1 is struck:
the rewrite was rebuilt against current main and force-pushed. What that
covered, verified by scanning the rewritten mirror end to end:

  BEFORE: 21 commit messages · 2 paths · 5,719 text blobs · 12 of 23
          archives carrying a term inside.
  AFTER:  0 · 0 · 0 · 0. No ZIP-shaped blob remains in history at all.

  Every archive artifact ever committed was removed rather than rewritten,
  because a term inside a compressed stream cannot be substituted in place:
  10 release ZIPs, 6 .docx, a favicon bundle, a Word lock file, and a whole
  committed unpacked release directory (123 files). Two branded PNGs went
  with them. The repository went from 982 MB to 141 MB.

  main lost exactly ONE commit, "Add the partnered Word Doc", which added
  nothing but a .docx. Its tree is byte-identical to what was live before
  the rewrite — checked by tree hash, not by eye. All 927 tags and all 16
  branches were rewritten and force-pushed; all 921 GitHub releases and
  their assets survived; the live site serves the same version it did.

  GitHub's auto-generated source archives — the part that could never be
  fixed without a rewrite — are regenerated and clean. Proven rather than
  assumed: the v3.63.393 archive now contains 18 occurrences of the
  REDACTED token that only exists because of this rewrite, and its file
  count matches the rewritten tag's tree exactly.


v290 — CAP WARNING AND RETRY COVERAGE SHIPPED (v3.63.514). Old #1 struck;
one item remains, and it is the one that is David's decision rather than a
task. Both halves done:

  (a) A length target past the Builder's known output ceiling warns at
      Launch with both numbers and the limit's provenance, stays silent when
      the cap is unknown, and never blocks.
  (b) The reject-and-learn retry was driven end to end for the first time,
      against a mock endpoint returning OpenAI's exact 400 wording. It found
      a real bug: the learned ceiling was recorded against cfg.model, the
      provider's DEFAULT model, instead of the model the request used. Every
      variant row and every row with a picked model differs from cfg.model,
      so the capped model never learned its ceiling and the rejection
      repeated every round while the picker showed an API-sourced number for
      a model that had never reported one.

Also shipped: the streaming setting finally has a UI control (Settings →
Workflow & Updates → "Stream responses"). It had existed since v3.63.499
reachable only from the console.

THE QUEUE IS NOW ONE ITEM, and that item needs David, not an assistant.
When it is resolved this file holds no numbered work at all — which is the
point, not a gap to fill. Do not invent entries to keep it populated.

v289 — ANTHROPIC + GEMINI STREAMING SHIPPED (v3.63.513). Old #1 struck;
items renumbered 1..2. The blocker named in that entry was real: the relay
Worker read the whole upstream response with .text() before replying, so
SSE could not have passed through it at all. Worker now streams the body
through and its test drives the real fetch handler against a stubbed
upstream, asserting the first frame is readable before the upstream closes.

NOT VERIFIED, and worth doing on the first real round: no request has gone
to the live Anthropic or Gemini APIs on the streaming path — this session
had no key for either. The mock reproduces both providers' documented SSE
shapes with frames split mid-payload, and everything downstream was driven
off it in a real browser, but a mock cannot stand in for a provider's real
byte timing. The tell is whether the "is writing... N chars so far" status
starts ticking; if it does not, streaming is not reaching the browser.

v288 — LOCAL-SERVER LIMITS SHIPPED (v3.63.512). Old #1 struck; items
renumbered 1..3. The declared half of the declared-vs-observed comparison
is populated for self-hosted AIs now, from responses WaxFrame was already
fetching. Verified against a live Ollama 0.34.2 rather than fixtures alone,
which is what caught the part that mattered: /api/tags publishes the
model's ARCHITECTURAL context length, and four of the ten models on that
machine carry a Modelfile num_ctx of 16384 sitting well under it. Shipping
/api/tags alone would have reported 256K at a server configured for 16K —
overstating the ceiling by 16x, which is the exact failure the entry's DONE
criterion was written to prevent. /api/show carries the override and is now
read on the deliberate refresh paths (one small POST per model, no model
load, ~420ms for ten models); it is deliberately NOT on the 60-second
connectivity probe.

LM Studio's /api/v0/models shape remains UNVERIFIED against a live
instance — the max_context_length / loaded_context_length pair is covered
by fixtures and by the pre-existing extractor code, not by a real run. Open
WebUI's nested .ollama shape is likewise fixture-only. Neither is a blocker;
both are worth a live pass next time those servers are up.

v287 — BACKLOG KNOCK-DOWN (2026-09-20, v3.63.511). Seven of the
eleven items shipped in one release. STRUCK, all verified in the
tree before removal:

  old #4  scrubFailureRecord Node fixture — tools/test-debug-
          redaction.mjs, 19 checks, wired as release-check stage 14.
          The first run of it found a real bug: js/wf-debug.js held a
          literal 0x08 BACKSPACE byte where the regex \b was meant, so
          the Bearer/Basic redaction rule had never matched anything
          since v3.63.493. Fixed, and the gate now rejects raw control
          characters anywhere in source.
  old #5  unreferenced .flac — deleted. Decision recorded: it is the
          freesound.org master, unused at runtime, re-downloadable from
          freesound.org id 232450, and it shipped 271 KB in every
          release ZIP. Note the v3.63.91 changelog had already CLAIMED
          this deletion; the file was still tracked, so a changelog
          line is not evidence a file is gone.
  old #6  @keyframes lineNumPulse — removed. Zero references anywhere
          in the tree, including JS template literals.
  old #7  :not(.setup-section-img) — exclusion removed, comment
          rewritten. Specificity drop from (0,2,0) to (0,1,0) verified
          harmless: nothing else sets position or z-index on a direct
          child of .fs-body-single, and all six setup screens were
          confirmed in-browser still computing position:relative /
          z-index:1 on their panels.
  old #8  reference docs self-dating to v3.63.403 — all four reviewed
          against the live tree, not just re-dated. Real drift found
          and corrected in three of them; the Prompts Reference's
          builder_refine text was ~55 lines behind the shipped prompt
          and was replaced from js/prompts.js.
  old #9  stale .claude/worktrees entries — nothing left to prune;
          git worktree prune reported nothing and the directory is
          empty. The old Dropbox path is gone from disk entirely.
  old #10 test-server-ai-eligibility.mjs — wired as release-check
          stage 15.

Items renumbered 1..4 with no gaps. The gate went from 17 stages to
19, and its stamp sweep now covers js/**/*.mjs and tools/**/*.mjs
instead of a hand-kept list in CLAUDE.md — the list had already let
tools/indexnow-ping.mjs rot ~90 releases behind.

v281 — HOUSEKEEPING PASS (2026-09-14). The item count had drifted
away from the amount of real work. Every claim below was verified
against the live repo before acting:

  STRUCK (already done, never removed):
    old #7  questionnaire — its own STATE said SHIPPED 2026-08-09
    old #10a model-limit half — shipped v3.63.494. Verified:
            provider-catalog.js:148 DEFAULT_OUTPUT_BUDGET = 32768,
            :309 body.max_tokens = resolveOutputBudget(model),
            ANTHROPIC_MAX_OUTPUT_TOKENS absent from js/ and tools/.
  MOVED OUT OF THE QUEUE (decisions and standing practice, not
  tasks — see the two sections after OPEN FEATURES):
    old #1 (ongoing housekeeping), #4 (DEFERRED by David
    2026-08-02), #6 (PARKED 2026-08-01), #9 (tooling gap; js/
    verified clean today).
  MERGED:
    old #10b + #12 — one job: local servers publish no output cap
    old #11 + #16  — both hang off knowing the cap: warn before,
                     recover after
  SPLIT:
    old #8 was six separate chores under one number. Now six
    numbered items, so the count reflects the work.
  RENUMBERED 1..11 with no gaps. The old numbering had no #5
  (descoped 2026-08-02) and had accumulated struck entries.

Net: 16 numbered items → 11, and the 11 are all real.

v282 — OVERNIGHT VERIFICATION SWEEP (2026-09-15). Old #3 (the
open-webui-setup.html CORS trap) SHIPPED in v3.63.497 and is struck;
items renumbered 1..10. Everything else on the list was re-verified
against the running app in real Firefox from file:// — 29 checks, 0
failures. Detail in the v3.63.498 changelog entry.

v283 — STREAMING SHIPPED (v3.63.499). Old #1 struck; items renumbered
1..9. Streaming covers the OpenAI shape only — every local server, every
gateway deployment and 8 of 10 built-in providers, including the path
that actually times out. Anthropic (CF Worker proxy) and Gemini (needs
:streamGenerateContent) still need doing; that is now item 9.


════════════════════════════════════════════════════════════
OPEN BUGS
════════════════════════════════════════════════════════════
None currently open.

════════════════════════════════════════════════════════════
OPEN FEATURES
════════════════════════════════════════════════════════════
Ordered by value.

1. Exercise PDF import against real-world PDFs
   ─────────────────────────────────────────────────────────
   WHAT:   v3.63.528 moved the hosted PDF engine to 6.3.289. It was
           verified against a generated two-page PDF — text, outline,
           page render, and the app's own import routine — and the
           portable file:// path was verified against the same file.
   WHY:    A generated PDF exercises the happy path only. The shapes
           that break PDF engines are the ones a generator does not
           produce: scanned pages with no text layer, encrypted or
           password-protected files, very large documents, unusual
           embedded fonts, and PDFs written by tools that bend the
           spec. A major-version engine change is exactly when those
           surface.
   STATE:  OPEN, blocked on David — needs real files, and the useful
           ones are his own. Nothing to code until it fails.
   WHERE:  js/app.js extractPDF (the import path) and the OCR
           fallback that renders pages when text extraction is sparse;
           tools/check-file-protocol.mjs for the portable path.
   DONE:   Several real PDFs imported through the app on waxframe.com
           — at minimum one scanned/image-only and one large multi-
           hundred-page file — with the extracted text checked for
           sanity. Any failure reported with the file's characteristics
           (NOT the file itself, and NOT its content — see §0 of the
           handover; a real work PDF must never reach this repo).


Ordered by value. Everything here is real work with a real
acceptance test. Items needing David rather than an assistant say so.

NOTES ON FORMAT
════════════════════════════════════════════════════════════
Every numbered entry above uses the same five-field structure:
WHAT, WHY, STATE, WHERE, DONE. Going forward, any new
backlog entry MUST follow that structure so the document
stays pickup-ready for any reader.

State vocabulary:
  OPEN      — known and ready to work on; no blockers.
  PARTIAL   — work has started, more remains; current
              state described in the entry.
  PARKED    — design is understood but not on a near-term
              release path; revisit later.
  DEFERRED  — paused waiting on an external input
              (user repro, upstream decision, etc.).

When an item ships, it moves to CHANGELOG.md and is
DELETED from this file. No "CLOSED" section here — the
changelog is the system of record for completed work.

Two habits this document keeps failing at, both fixed in v281:
  • Shipped items must be DELETED the day they ship. Old #7 sat
    here marked SHIPPED for over a month.
  • Decisions and standing practice do not belong in a numbered
    queue — they inflate the count and can never be closed.
    Put them in STANDING PRACTICE.
