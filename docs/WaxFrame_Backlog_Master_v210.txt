WaxFrame Backlog Master v210
2026-06-04

State: v3.63.141 Pro shipped on main. Tier classifier rewritten to mirror recommendForDefault's structure. The v3.63.139/.140 implementation was a parallel re-implementation that bypassed the established model-fetch + fallback + asking-model infrastructure — every problem David's bundles surfaced traced back to that architectural drift. v3.63.141 fixes:

1. Model list: fetchModelsForProvider(provider) for defaults (knows each provider's actual discovery shape, including the documented "Perplexity has no /v1/models endpoint" case), MODEL_FALLBACKS as the canonical no-discovery fallback (Mistral/Cohere/Together/Jamba aren't in MODEL_FILTERS so they hit fetchModelsForProvider's early-return null, then fall through to MODEL_FALLBACKS). For customs, fetchModelsFromEndpoint stays since that IS their actual discovery path. New modelSource field (live-fetch / custom-endpoint / model-fallbacks) persisted with every cached classification so the bundle shows where each tier came from.

2. Asking-model ladder: viable[0] (newest filtered) → stableFallback (MODEL_FALLBACKS entry present in the live list) → cfg.model (user's currently-selected) → models[0]. Mirrors recommendForDefault exactly. Closes David's chicken-and-egg: classifier no longer inherits the bad behavior of whatever model happens to be in the production hive slot (often a reasoning model that buries answers in CoT).

3. Dev-toolbar 🐝 Classify Tiers button always forces refresh. Prior default served cache and only re-ran providers without a cached entry, which left v3.63.139's stale picks in place even after v3.63.140's live-fetch fix shipped (because the button never re-ran the 4 already-cached providers).

4. Prompt anchor: BALANCED and THINKER slots explicitly forbid models below ~7B parameters. Catches v3.63.140's Together AI mis-classification that put arize-ai/qwen-2-1.5b-instruct (1.5B distilled) in BALANCED.

5. Bundle diagnostics: every cached classification carries modelSource + askingModel so when a result looks wrong, the bundle shows exactly where the model list came from and which model produced the answer.

Next test loop: David rotates the keys leaked in v3.63.139, reloads v3.63.141, clicks 🐝 Classify Tiers (now always force-refreshes all 10 providers), bundles, sends. Expected: all 10 providers classified with modelSource visible per provider, ChatGPT/Claude/Gemini/Grok with fresh picks (no longer stale from v3.63.139), Perplexity/Mistral/Cohere/Together/Jamba working via MODEL_FALLBACKS chain, Together AI no longer putting 1.5B models in BALANCED. Then evaluate whether prompt iteration is needed or whether we move to Step 2 (Perplexity verification pass).

v3.63.140 state still holds: bundle keys redacted, custom AIs walked, force-fetch models, per-provider failure log. v3.63.139 state still holds: dynamic tier classifier + dev-toolbar simplification. v3.63.138 state still holds: Templates Phase 1 trio + length-floor convergence gate + Mistral 429 warning. v3.63.137 state still holds: per-platform Product Review variant arc complete. Sitemap lastmod 2026-06-04. PLAYBOOK TEST MASTER v37 is the live test-tracking doc.

PRIOR STATE (v3.63.140, retained for context until next release reshuffles the margin):
SECURITY fix — Bundle for Scout no longer exports plaintext API keys. Four classifier coverage fixes: walks aiList not DEFAULT_AIS (catches customs), force-fetches the live model list, captures per-provider failures, bundle tier-cache read walks aiList too.

PRIOR STATE (v3.63.139, retained for context until next release reshuffles the margin):
Hive Profiles step 1 — dynamic tier classifier (CHEAP / BALANCED / THINKER / FAST). Dev-toolbar surface simplified: Deep Dive auto-on at dev-mode unlock, 📦 Bundle for Scout single-file export, retired View Captures + View Tiers modals.

PRIOR STATE (v3.63.138, retained for context until next release reshuffles the margin):
Templates page Phase 1 follow-ups complete the trio + majority-convergence length-floor gate + Mistral 429 proactive warning.

Open Bugs

(none currently tracked)

Open Features

- Hive Profiles step 2: Perplexity verification pass. After classifications validate across all 10 providers (now expected possible in v3.63.141), ship the bundle to Perplexity with a web-search verification prompt that confirms / revises / flags-as-unknown each pick against current provider pricing pages.

- Hive Profiles step 3: storage + resolver + seeded starter profiles + custom user-named profiles.

- Hive Profiles step 4: dropdown UI above the bee grid + hive-profiles.html docs page.

- Restore-from-Checkpoint diff-and-pick UI — PARKED.

- Hive parallel fan-out RPS-awareness (active throttle counterpart to v3.63.138's passive warning).

- Model recommender filter-awareness warning.

- Failover Builder behavior — single-round CONTENT_FILTERED handling.

- Builder-incapable family list expansion — watch-list. Currently just Jamba.

- Pause/resume at reviewer seam.

- File System Access API exploration.

- Provider Catalog foundation (parked).

- CSS dead-selector cleanup branch.

- CSS rule consolidation sweep.

- Repo housekeeping — `docs/` folder.

- Triage CodeQL + Dependabot findings.

Observations

- SHIPPED — v3.63.141 classifier architectural alignment. The v3.63.139/.140 classifier reinvented infrastructure that already existed in recommendForDefault; every coverage gap and chicken-and-egg failure traced back to that. David called out the band-aid pattern after his v3.63.140 bundle (4 of 10 providers still stale, 2 of 10 silently failing, 1 picking a 1.5B model for BALANCED) — investigation surfaced that the recommender already solves model-fetch fallback (MODEL_FALLBACKS chain), asking-model selection (viable → stableFallback → cfg.model ladder), and provider-specific discovery quirks (Perplexity has no /v1/models). v3.63.141 mirrors that pattern exactly instead of patching symptoms. Lesson: when adding a new feature that touches an existing subsystem, audit the subsystem for already-solved problems before implementing parallel infrastructure.

- 2026-06-04 — David's v3.63.140 bundle surfaced that 4 of 10 providers were still serving v3.63.139-era stale cache (chatgpt/claude/gemini/grok) because the dev-toolbar button defaulted force=false. Three previously-missing providers (DeepSeek/Cohere/Together AI/Jamba) DID classify in v3.63.140 — coverage doubled from 4 to 8 — but the run also revealed that the v3.63.139 prompt didn't anchor capability requirements: Together AI's classifier picked arize-ai/qwen-2-1.5b-instruct (1.5B distilled) for BALANCED. Cohere correctly routed command-a-reasoning-08-2025 to THINKER (prompt working as designed when models follow naming conventions). Jamba correctly used NONE for THINKER (no reasoning variant exists). DeepSeek's /v1/models endpoint doesn't list v4-pro even though David uses it in his hive — real provider-API gap, classifier correctly reported deepseek-v4-flash only.

- SHIPPED — v3.63.140 security fix: Bundle for Scout no longer exports plaintext API keys.

- SHIPPED — v3.63.139: Hive Profiles step 1 dynamic tier classifier + dev-toolbar simplification.

- 2026-06-04 — David's reasoning-token economy data across two real runs: token-burn scales with task complexity. DeepSeek-v4-pro: 14× ratio on a complex Target review, 1.67× on a simple cookie recipe. Cohere command-a-plus: 7.4× ratio consistent across both runs. Gemini 3.1-pro-preview: hides ~1,500-2,800 reasoning tokens per call under usageMetadata.totalTokens (separate from completion_tokens — the bills get bigger than the visible output suggests). Hive Profiles arc is the answer — give users a one-click "Cheap" preset that excludes reasoning models entirely. Step 1 lands the classification infrastructure; step 3 wires the actual preset resolution.

- 2026-06-04 — release-check validator caught no drift across v3.63.135 through v3.63.141 (seven green ceremonies). ROI continues to compound.

- 2026-06-04 — SEO measurement infrastructure live since v3.63.128.

- cleaner.html preserved in David's local working tree.

- Adoption-friction note: David surfaced "zero sales after months of build" 2026-06-01, reaffirmed 2026-06-04. Engineering merit answers stand independent of conversion data.
