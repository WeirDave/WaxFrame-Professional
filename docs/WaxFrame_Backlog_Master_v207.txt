WaxFrame Backlog Master v207
2026-06-04

State: v3.63.138 Pro shipped on main. Four backlog quick wins land together. (1) Templates page Phase 1 follow-ups complete the trio Phase 1 was missing — sticky search/filter bar (matches against name + description + category + document type + goal fields, collapses empty sections under the filter), per-card View Details expander (Audience / Outcome / Scope / Tone / Constraints visible inline, scaffold expander stays below), bulk Export All customs as .zip (uses existing lib/jszip.min.js; each file inside is the same JSON envelope the per-template exporter produces, drop-in re-importable). (2) Majority-convergence length-floor gate at app.js:14449 — when project has a length constraint (range or target) and the document is out of range AND there are holdouts, MAJORITY convergence is deferred so the holdouts get another round to land. The holdouts in this scenario almost always include AIs pushing for length expansion (or trim); the prior path silenced their signal by declaring convergence on top of them. Unanimous unchanged. Length Guard override opts out. Logs "📏 Majority convergence deferred…" + toast. Closes David's TripAdvisor R3 observation from v206 (478 words vs 500 floor; 4 holdouts all proposing length-related expansion). (3) Mistral 429 proactive warning at app.js:15249 — after each Mistral response, smallest x-ratelimit-remaining-* requests-counter header triggers a console warn + yellow ~N-req-left chip on the bee card when below 3 remaining. Throttled per-AI per 20s; chip clears ~12s. Helps free-tier Mistral parallel-fan-out scenarios get advance notice before the 1-rps / 6-rpm wall.

v3.63.137 state still holds: per-platform Product Review variant arc complete (Best Buy added). v3.63.136 state still holds: Walmart / Target / Newegg per-platform templates. v3.63.135 state still holds: templates page quick wins (per-platform recipe ref, Duplicate, Recently-saved sort). v3.63.134 state still holds: GitHub Actions release-ceremony validator. v3.63.133 state still holds: Deep Dive capture dual-homed. v3.63.132 state still holds: Builder-incapable model filter (Jamba) + BUILDER_TRUNCATED. v3.63.131 state still holds: audit polish. v3.63.130 state still holds: selective EXPORT modal + scrubbed retirement. v3.63.129 state still holds: Backup → Checkpoint rename. Sitemap lastmod 2026-06-04. PLAYBOOK TEST MASTER v37 is the live test-tracking doc.

PRIOR STATE (v3.63.137, retained for context until next release reshuffles the margin):
Best Buy product review template added (product-review-bestbuy, icon 🔵) with David's verified constraints: title 5-50 chars, body 50-5,000 chars, single overall 1-5 star rating with no secondary star prompts. Normal body range 1,500-3,000 pre-selected. PER-PLATFORM VARIANT ARC NOW COMPLETE — all five US-major retail platforms covered: Amazon (v3.63.94 measured baseline), Walmart / Target / Newegg (v3.63.136), Best Buy (v3.63.137).

PRIOR STATE (v3.63.136, retained for context until next release reshuffles the margin):
Three new per-platform Product Review templates — Walmart / Target / Newegg — based on David's verified constraints. Walmart: 50-char title cap, 3,000-char body cap, five secondary star prompts addressed. Target: no title cap, 3,500-char body cap, three secondary star prompts. Newegg: structurally different — three separate input fields (Pros / Cons / Overall Review) each ~4,000 char cap, structured output via section headers, uses eggs not stars.

PRIOR STATE (v3.63.135, retained for context until next release reshuffles the margin):
Templates page quick wins. Per-platform recipe reference block in js/templates.js. Per-card 📋 Duplicate action on My Templates customs. "Recently saved" sort dropdown on My Templates.

PRIOR STATE (v3.63.134, retained for context until next release reshuffles the margin):
GitHub Actions release-ceremony validator (.github/workflows/release-check.yml + tools/release-check.mjs). First run caught 18 silently-failing CSS token references. Validator caught its first real-world drift during v3.63.135 ceremony (package.json forgot to bump).

Open Bugs

(none currently tracked)

Open Features

- Restore-from-Checkpoint diff-and-pick UI — turn the current blunt overwrite into a comparison + selective-import flow. STATUS: PARKED — gated on user feedback once the selective-export modal has actual users.

- Custom Templates Phase 2 + Phase 3 — full editors behind the scaffold buttons that landed in v3.63.97. PHASE 2 (Create Blank), PHASE 3 (Import .json), PHASE 1B (hive auto-reconcile on apply). NEXT UP per David's 2026-06-04 direction.

- Hive parallel fan-out RPS-awareness. Optional per-provider concurrency cap so paid Mistral on a low tier doesn't 429 itself with parallel fan-out. v3.63.138 adds passive warning; this is the active throttle.

- Model recommender filter-awareness warning — when the recommender's pick has been flagged in the current session's ringBuffer with CONTENT_FILTERED, surface a ⚠️ next to that model in the Change Builder dropdown.

- Failover Builder behavior — primary Builder hits CONTENT_FILTERED / EMPTY_RESPONSE → auto-failover to a configured Failover Builder for that one round only. Settings → Auto Mode dropdown exists; this entry is about the BEHAVIOR for single-round CONTENT_FILTERED (today the dropdown only fires on failure-streak halts).

- Builder-incapable family list expansion — watch-list. Currently just Jamba. As other broken-as-Builder families surface, extend BUILDER_INCAPABLE_FAMILIES regex.

- Pause/resume at reviewer seam — finer-grained pause INSIDE a round, between reviewers finishing and the builder firing.

- File System Access API exploration — true file persistence vs download pile-up.

- Provider Catalog + Hive Profiles Foundation — two-layer provider model. 4-phase sequence: starter flag → "+ Add from Catalog" modal → catalog expansion (Cloud then Local) → Hive Profiles as saved snapshots.

- CSS dead-selector cleanup branch — ~155 flagged selectors. Static grep insufficient; dedicated branch, phased deletion, visual smoke test gate.

- CSS rule consolidation sweep — 6 cases worth consolidating.

- Repo housekeeping — `docs/` folder. Continued trim as needed.

- Triage CodeQL + Dependabot findings once first scans complete. Blocked on David pulling Security tab findings.

(Note: "Templates page Phase 1 follow-ups" REMOVED from Open Features — trio complete in v3.63.138 with search/filter, View Details, and Export All ZIP. The Phase 1 arc that started in v3.63.97 (scaffold buttons) → v3.63.135 (per-card Duplicate + Recently-saved sort) → v3.63.138 closes here. Phase 1B (hive auto-reconcile) folded under Custom Templates Phase 2/3.)

(Note: "Mistral 429 proactive warning" REMOVED from Open Features — passive header-read warning shipped in v3.63.138. The Hive parallel fan-out RPS-awareness entry above is the still-open active-throttle counterpart.)

Observations

- SHIPPED — Templates page Phase 1 trio complete (v3.63.138). Search/filter bar + View Details expander + bulk Export All as .zip. Phase 1 was the scaffold-buttons + per-card actions + sortable list arc kicked off in v3.63.97 and incrementally filled in across v3.63.135 and now v3.63.138. Custom Templates Phase 2 (Create Blank editor) and Phase 3 (Import .json) remain as the next Templates arc.

- SHIPPED — Majority-convergence length-floor gate (v3.63.138). Closes David's 2026-06-04 TripAdvisor observation. The previous behavior surfaced a length-guard prompt AFTER majority convergence was declared, which gave the user a "Block convergence" option — David clicked Accept because the celebration framing suggested the run was done. The new behavior never declares majority convergence in the first place when the document is out of range; the round proceeds normally so the holdouts' length-related suggestions land in the Builder phase. Unanimous unchanged — no holdouts means no signal left to leverage; the prompt still asks.

- SHIPPED — Mistral 429 proactive warning (v3.63.138). Reads x-ratelimit-remaining-* headers on every Mistral response, surfaces a console warn + yellow ~N-req-left chip on the bee card when remaining < 3. Throttled per-AI per 20s. Free-tier Mistral parallel-fan-out gets advance notice before the 1-rps / 6-rpm wall. Active throttle (concurrency cap) remains open as a separate entry.

- SHIPPED — Per-platform Product Review template arc complete (v3.63.137 ships Best Buy, completing Amazon + Walmart + Target + Newegg + Best Buy). All five US-major retail platforms have dedicated templates with verified constraints baked in.

- 2026-06-04 (during v3.63.135 ceremony) — the v3.63.134 release-check validator caught its FIRST REAL DRIFT IN THE WILD. Cache-bust sweep missed package.json. Validator surfaced "package.json version mismatch" on the pre-commit run. v3.63.136 + v3.63.137 + v3.63.138 ceremonies stayed green throughout — lesson learned. ROI confirmed across four releases now.

- 2026-06-04 — SEO measurement infrastructure live since v3.63.128. Google Search Console + Bing Webmaster Tools imported. First signals expected ~2 weeks from setup.

- cleaner.html preserved in David's local working tree for the eventual in-app normalizeHive() port. Not committed to repo.

- Adoption-friction note: David surfaced "zero sales after months of build" 2026-06-01, reaffirmed 2026-06-04. Engineering merit answers stand independent of conversion data.
