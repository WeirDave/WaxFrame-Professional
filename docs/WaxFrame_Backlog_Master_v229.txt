WaxFrame Backlog Master v229
2026-06-05

State: v3.63.160 Pro shipped on main. First of four planned releases (v3.63.160-163) that complete the Builder/Worker meld. Four scopes in this release:

1. Intro paragraph rewrite. The "Each AI in your hive acts as a reviewer..." 3-paragraph block on the Set up your hive screen was wordy. David flagged it as "not fluid". Rewritten tighter: hive-as-team framing, bolded 2-AI minimum + 3+ recommendation, direct CTA.

2. Change Builder modal restructure. Previously used a chip-grid + shared model dropdown + separate "Set as Builder" commit button. Replaced with row-based methodology matching the Set up your hive page: each candidate AI gets a .cb-row with icon + name + Model: label + compact native select + Builder button. Builder button has the same 4 states (is-active gold light-tint, is-pickable gray, is-incapable yellow, placeholder). Clicking Builder commits immediately + closes modal. Per-row dropdown's onchange persists model via saveModelForAI. Legacy functions selectBuilderCandidate / confirmBuilderFromModal retained as no-op stubs. Modal title image bumped from v3.png to v4.png.

3. help.html review. Grep confirmed no Worker Bees / Setup / Builder page references. The Dump Provider Models section is a standalone diagnostic with no methodology overlap. No changes needed.

4. Sidebar block + Hive Profile bar light-mode fix. Both were rgba(0,0,0,0.25) which read as grey over the amber honeycomb backdrop in light mode. Switched to the work-screen .work-panel-header.honeycomb-header pattern: rgba(0,0,0,0.55) + 6px backdrop-filter + matching border/shadow. Text colors forced to light in both themes. Consistent dark-navy branded panel treatment now matches THE HIVE, WORKING DOCUMENT, CONFLICTS, LIVE CONSOLE headers on the work screen.

Up next (v3.63.161): User Manual rewrite. waxframe-user-manual.html sidebar links, step table, inline references throughout — all reference 5-step flow with separate Builder screen. Need to renumber to 4 steps (Setup 1 = Set up your hive, 2 = Project, 3 = Reference, 4 = Starting Document) and remove Builder step references.

v3.63.159 state still holds: bee mascot tooltips (Wally, Bob, Art, Paul, Ralph, Scout, Andy, Henry, Pete, Theo) + top count chip numbers in gold.

PRIOR STATE (v3.63.159, retained for context until next release reshuffles the margin):
Bee mascot tooltips via centralized BEE_NAMES mapping + applyBeeTooltips walker. Top count chip strong elements in var(--accent) gold for consistency with .ai-setup-group-count and bulk-select toolbar.

PRIOR STATE (v3.63.158, retained for context until next release reshuffles the margin):
Sidebar Jump-to-AI scroll regression fix. Replaced window.scrollTo with row.scrollIntoView + scroll-margin-top:80px + requestAnimationFrame timing.

PRIOR STATE (v3.63.157, retained for context until next release reshuffles the margin):
Polish pass. Card-click bug confirmed fixed via window binding. Builder button click no longer expands row. Light-tint active Builder. Custom count gold inline. Hive Profile note rewording.

Open Bugs

(none currently tracked outside the active task list)

Open Features / Tasks — Builder/Worker meld finalization roadmap

- v3.63.161 (next): User Manual rewrite (waxframe-user-manual.html) — sidebar links, step table, inline references, Builder screen removal, Setup numbering 5→4
- v3.63.162: README.md (alt text + body references + screenshot regen flagging) + landing pages (ai-cover-letter-editor, ai-resume-review, ai-business-proposal, document-playbooks, prompt-editor, templates) + info modals in index.html + api-details.html walkthrough references
- v3.63.163: Code cleanup — remove #screen-builder DOM from index.html, remove continueFromBuilder legacy stub, retire worker-bees-prototype.html, archive docs/worker-bees-audit.md, CSS dead-selector cleanup pass (.bees-sidebar-legend*, .ai-setup-builder-chip*, .ai-builder-affordance*)

Remaining smaller tasks:
- #10 Hive Profiles: "Save current as named profile" (named user profiles)
- #12 Hive Profiles: dedicated docs page (hive-profiles.html)
- #24 Worker Bees rework: drop sub-1366px responsive code
- #27 Worker Bees rework: 3-group ordering (defaults / customs / servers)
- #35 Auto-classify tier picks always (not click-to-classify)
- #36 Worker Bees rework: server provider group label
- Per-row "Manual override" tag — needs delta tracking
- Per-tier WHY text: classifier currently produces one overall WHY
- WaxFrame_Builder_v3.png pruning candidate
- Restore-from-Checkpoint diff-and-pick UI — PARKED
- Hive parallel fan-out RPS-awareness
- Model recommender filter-awareness warning
- Failover Builder behavior — single-round CONTENT_FILTERED handling
- Builder-incapable family list expansion
- Pause/resume at reviewer seam
- File System Access API exploration
- Provider Catalog foundation (parked)
- CSS rule consolidation sweep
- Repo housekeeping — docs/ folder
- Triage CodeQL + Dependabot findings
- Rules reference updates

Observations

- 2026-06-05 — Change Builder modal restructure proves the pattern from Set up your hive transfers cleanly to other surfaces. The same _buildCompactModelSelect + Builder button stack handles both the inventory screen rows and the modal candidate rows with one shared implementation. Consistency-via-shared-component instead of consistency-via-style-copy.

- 2026-06-05 — Light-mode dark panel treatment lesson: `rgba(0,0,0,0.25)` is too thin for cross-theme reliability. The work-screen pattern of `rgba(0,0,0,0.55) + backdrop-filter: blur(6px)` produces a solid-looking branded panel that works in both themes. Should be the canonical "intentional dark panel" pattern going forward.

- 2026-06-05 — release-check validator caught no drift across v3.63.135 through v3.63.160 (twenty-six ceremonies).

- 2026-06-05 — SEO measurement infrastructure live since v3.63.128.

- cleaner.html preserved in David's local working tree.

- Adoption-friction note: David surfaced "zero sales after months of build" 2026-06-01. The Worker Bees arc continues smoothing friction. The intro paragraph rewrite is conversion-relevant — the previous wordy block was the first thing a new user read on the setup screen.
