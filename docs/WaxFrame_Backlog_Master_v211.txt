WaxFrame Backlog Master v211
2026-06-04

State: v3.63.142 Pro shipped on main. Provider link audit fix release. David caught a broken Grok billing link on the AI API Pricing page and asked the right question — had Scout actually checked any of the URLs cited across the site? No. Full audit found 11 stale URLs across 7 files. v3.63.142 fixes everything verified-broken:

1. Anthropic console host migration: console.anthropic.com → platform.claude.com across api-links.js (the central API_CONSOLE_URLS + API_USAGE_URLS registry), index.html, api-details.html, waxframe-user-manual.html, what-are-tokens.html, ai-api-pricing.html PROVIDERS array, and tools/pricing-worker/data/pricing-seed.json. 302 redirect confirmed for both /settings/billing and /settings/keys. All occurrences swept.

2. Grok billing path doesn't exist: console.x.ai/billing was a guess. xAI's actual console paths are team-UUID-scoped (e.g. console.x.ai/team/<uuid>/usage), so a hardcoded billing path can't work. Fixed by pointing at the console root console.x.ai and letting the user navigate to Billing inside. Verified against docs.x.ai/console/billing.

3. Mistral billing was mislabeled: the pricing-seed billingUrl field pointed at console.mistral.ai/api-keys, which is Mistral's API-keys page, not billing. Mistral split billing to admin.mistral.ai. Fixed to https://admin.mistral.ai/organization/billing. The console.mistral.ai/api-keys references in api-details.html (for the "get an API key" walkthrough) are intentionally preserved — that page IS the API-keys page and the context is correct.

URLs that look 403 but are fine: Gemini, ChatGPT, Cohere, DeepSeek, Together AI, Perplexity all returned 403 to bot fetches but their URL shapes match each provider's current docs. Auth-gating, not broken. Left alone.

Lesson learned added to observations: any external URL added to user-facing copy should be verified at write-time, not assumed-correct based on the provider's last-known structure. Worth adding to the rules reference for future link writes.

v3.63.141 state still holds: Tier classifier architectural alignment with the recommender. v3.63.140 state still holds: bundle keys redacted + classifier walks aiList + force-fetch + per-provider failure log. v3.63.139 state still holds: dynamic tier classifier + dev-toolbar simplification. Sitemap lastmod 2026-06-04. PLAYBOOK TEST MASTER v37 is the live test-tracking doc.

PRIOR STATE (v3.63.141, retained for context until next release reshuffles the margin):
Tier classifier rewritten to mirror recommendForDefault's structure. Uses fetchModelsForProvider for defaults + MODEL_FALLBACKS fallback chain (handles Perplexity's no-/v1/models case + Mistral/Cohere/Together/Jamba "not in MODEL_FILTERS" case). Asking-model ladder: viable[0] -> stableFallback -> cfg.model -> models[0]. Dev-toolbar button always force-refreshes. Prompt capability anchor for BALANCED + THINKER. Bundle gains modelSource + askingModel diagnostic provenance.

PRIOR STATE (v3.63.140, retained for context until next release reshuffles the margin):
SECURITY: Bundle for Scout sanitizes LS_HIVE.keys before serialization. Classifier walks aiList not DEFAULT_AIS (catches customs). Force-fetches live model list. Per-provider failure log included in bundle.

PRIOR STATE (v3.63.139, retained for context until next release reshuffles the margin):
Step 1 of Hive Profiles arc — dynamic per-provider tier classifier. Dev-toolbar surface: 🐝 Classify Tiers + 📦 Bundle for Scout.

Open Bugs

(none currently tracked)

Open Features

- Hive Profiles step 2: Perplexity verification pass. Architecture is solid as of v3.63.141; coverage is 10/10 as of David's most recent bundle. Step 2 verifies the picks against live provider docs via Perplexity web search. NEXT UP.

- Hive Profiles step 3: storage + resolver + seeded starter profiles (Cheap / Balanced / Heavy thinkers / Speed-first) + custom user-named profiles.

- Hive Profiles step 4: dropdown UI above the bee grid + hive-profiles.html docs page.

- Restore-from-Checkpoint diff-and-pick UI — PARKED.

- Hive parallel fan-out RPS-awareness (active throttle counterpart to v3.63.138's passive warning).

- Model recommender filter-awareness warning.

- Failover Builder behavior — single-round CONTENT_FILTERED handling.

- Builder-incapable family list expansion — watch-list.

- Pause/resume at reviewer seam.

- File System Access API exploration.

- Provider Catalog foundation (parked).

- CSS dead-selector cleanup branch.

- CSS rule consolidation sweep.

- Repo housekeeping — `docs/` folder.

- Triage CodeQL + Dependabot findings.

- Rules reference update: add a "verify every external URL before shipping" guideline to docs/WaxFrame_Rules_Reference.txt. v3.63.142's audit caught 11 stale URLs that shipped silently because nobody clicked them. A short note in the rules + a WebFetch-or-WebSearch verification step would have caught most of them at write-time.

Observations

- SHIPPED — v3.63.142 provider link audit. 11 stale URLs across 7 files. Root causes: provider host renames (Anthropic console → platform.claude.com), path restructures (Grok billing UUID-scoped), label mix-ups (Mistral billing pointing at api-keys page). All verified-broken URLs fixed; auth-gated URLs that look 403 but are correct were audited via WebSearch against provider docs.

- 2026-06-04 — David caught the Grok billing link broken and asked Scout if any of the cited URLs had been verified. Scout had not. The audit pattern (verify with WebFetch when possible; cross-check with WebSearch against provider docs when 403; only ship URLs verified at write-time) should be added to the rules reference. The user-facing damage from stale URLs is low per individual instance but compounds across a docs site of ~30 external links.

- SHIPPED — v3.63.141 classifier architectural alignment. David's v3.63.141 bundle confirmed: 10 of 10 providers classified, zero failures. modelSource correctly tracks live-fetch / model-fallbacks / custom-endpoint per provider. Capability anchor caught Together AI's 1.5B-in-BALANCED issue. Perplexity and Mistral now classify correctly via MODEL_FALLBACKS chain.

- 2026-06-04 — v3.63.141 bundle surfaced four remaining classification-quality issues that are AI-level (not architecture): (1) DeepSeek THINKER=null even though v4-pro is a reasoning model — name convention "pro" alone wasn't strong enough signal; (2) Together AI WHY field hallucinated, naming models not in any of the picks; (3) Cohere BALANCED picked c4ai-aya-expanse-32b (multilingual) instead of command-a-plus; (4) Grok still picks grok-build-0.1 for cheap. These are exactly what Hive Profiles step 2 (Perplexity verification) is designed to catch — prompt iteration alone can't close them.

- SHIPPED — v3.63.140 security fix: Bundle for Scout no longer exports plaintext API keys.

- SHIPPED — v3.63.139: Hive Profiles step 1 dynamic tier classifier + dev-toolbar simplification.

- SHIPPED — v3.63.138: Templates Phase 1 trio + length-floor convergence gate + Mistral 429 warning.

- 2026-06-04 — release-check validator caught no drift across v3.63.135 through v3.63.142 (eight green ceremonies). ROI continues to compound.

- 2026-06-04 — SEO measurement infrastructure live since v3.63.128.

- cleaner.html preserved in David's local working tree.

- Adoption-friction note: David surfaced "zero sales after months of build" 2026-06-01, reaffirmed 2026-06-04. Engineering merit answers stand independent of conversion data.
