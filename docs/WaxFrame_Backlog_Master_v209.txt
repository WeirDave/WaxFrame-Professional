WaxFrame Backlog Master v209
2026-06-04

State: v3.63.140 Pro shipped on main. SECURITY release + Hive Profiles classifier coverage fixes. The v3.63.139 Bundle for Scout export was leaking plaintext API keys via LS_HIVE.keys — David caught it on his first real bundle send. v3.63.140 sanitizes the export (`keys` → `[REDACTED — N keys stripped]`, customAIConfigs[*]._key → `[REDACTED]`, new `_waxframe_keys_redacted: true` envelope marker). Anyone who clicked Bundle for Scout in v3.63.139 needs to rotate their keys; David rotated his on 2026-06-04. Same release fixes four classifier coverage bugs surfaced by David's two v3.63.139 test bundles: (1) classifier runner now walks all keyed providers via aiList, not just DEFAULT_AIS — DeepSeek/Cohere/Together AI/Jamba were silently skipped before; (2) force-fetches the live model list per classification — stale waxframe_models_<provider> cache was producing picks from outdated model catalogs (ChatGPT v3.63.139 classified gpt-5.4-* while the hive was running gpt-5.5 successfully); (3) per-provider failure log captured and included in the bundle under `tierClassificationErrors`, so Mistral/Perplexity-style silent failures now leave a paper trail with stage tag (no-key / model-fetch / empty-model-list / all-models-filtered / http-error / empty-response / parse-failed / all-slots-empty / classifier-exception / runner-exception) and detail string; (4) bundle's tier-cache read now also walks aiList not just DEFAULT_AIS so customs land in the export when classified.

Iteration plan: v3.63.139's classifier prompt UNCHANGED in v3.63.140 — coverage and security fixes only. Next test run isolates whether the four coverage fixes are enough or whether the prompt also needs tightening. Step 2 (Perplexity verification pass) lands after we have full-coverage classifications across all 10 of David's keyed providers.

v3.63.139 state still holds: dynamic tier classifier + dev-toolbar simplification (Deep Dive auto-on at dev-mode unlock, 📦 Bundle for Scout single-file export, retired View Captures + View Tiers modals). v3.63.138 state still holds: Templates page Phase 1 trio + majority-convergence length-floor gate + Mistral 429 proactive warning. v3.63.137 state still holds: per-platform Product Review variant arc complete. v3.63.136 state still holds: Walmart/Target/Newegg per-platform templates. v3.63.135 state still holds: templates page quick wins. v3.63.134 state still holds: GitHub Actions release-ceremony validator. Sitemap lastmod 2026-06-04. PLAYBOOK TEST MASTER v37 is the live test-tracking doc.

PRIOR STATE (v3.63.139, retained for context until next release reshuffles the margin):
Step 1 of Hive Profiles arc — dynamic per-provider tier classifier (CHEAP / BALANCED / THINKER / FAST). Surfaces in dev toolbar via 🐝 Classify Tiers button. 📦 Bundle for Scout consolidates ringBuffer + tier classifications + project checkpoint + version metadata into one JSON download. Deep Dive auto-enables on dev-mode unlock. View Captures + View Tiers viewer modals retired.

PRIOR STATE (v3.63.138, retained for context until next release reshuffles the margin):
Templates page Phase 1 follow-ups complete the trio (search/filter bar, View Details expander, bulk Export All customs as .zip). Majority-convergence length-floor gate at app.js:14449. Mistral 429 proactive warning at app.js:15249.

PRIOR STATE (v3.63.137, retained for context until next release reshuffles the margin):
Best Buy product review template added. PER-PLATFORM VARIANT ARC NOW COMPLETE — all five US-major retail platforms covered.

Open Bugs

(none currently tracked)

Open Features

- Hive Profiles step 2: Perplexity verification pass. After all providers self-classify (now with full coverage in v3.63.140), ship the bundle to Perplexity with a web-search verification prompt that confirms / revises / flags-as-unknown each pick against current provider pricing pages. NEXT UP once we see v3.63.140's classifier coverage in a real run.

- Hive Profiles step 3: storage + resolver + seeded starter profiles (Cheap / Balanced / Heavy thinkers / Speed-first) + custom user-named profiles. localStorage key waxframe_hive_profiles, replace-not-augment semantics, honors current hive-size flexibility (2 / 6 / 10).

- Hive Profiles step 4: dropdown UI above the bee grid + hive-profiles.html docs page.

- Restore-from-Checkpoint diff-and-pick UI — PARKED.

- Hive parallel fan-out RPS-awareness (active throttle counterpart to v3.63.138's passive warning).

- Model recommender filter-awareness warning — ⚠️ on Change Builder dropdown when CONTENT_FILTERED was flagged this session.

- Failover Builder behavior — single-round CONTENT_FILTERED handling.

- Builder-incapable family list expansion — watch-list. Currently just Jamba.

- Pause/resume at reviewer seam.

- File System Access API exploration.

- Provider Catalog foundation (parked; Hive Profiles half of the original v207 entry is the active arc).

- CSS dead-selector cleanup branch — ~155 flagged selectors.

- CSS rule consolidation sweep — 6 cases.

- Repo housekeeping — `docs/` folder.

- Triage CodeQL + Dependabot findings.

Observations

- SHIPPED — v3.63.140 security fix: Bundle for Scout no longer exports plaintext API keys. Discovered by Scout (Claude Code) on David's first v3.63.139 bundle send. Keys for all 10 of David's providers were in the file; he rotated them 2026-06-04. The bug was a one-line oversight in v3.63.139's bundleForScout — checkpoint.LS_HIVE was copied verbatim from localStorage with no sanitization pass. Fix: sanitize before serialize, replace .keys object with a redaction marker, also redact customAIConfigs[*]._key. Lesson: any export path that dumps localStorage needs a key-stripping helper applied at the boundary. Worth a wider audit of other export paths (backupSession / Save Checkpoint / template export) — none of them have user-typed keys today, but if any future export path crosses the boundary again the same pattern will bite.

- SHIPPED — v3.63.140 classifier coverage fixes: walks aiList not DEFAULT_AIS (catches customs), force-fetches the live model list (no more stale picks), captures per-provider failures into a structured error log (Mistral/Perplexity silent failures now visible). Diagnostic information now flows back to Scout via the bundle's tierClassificationErrors field.

- SHIPPED — v3.63.139: Hive Profiles step 1 dynamic tier classifier + dev-toolbar simplification.

- SHIPPED — v3.63.138: Templates Phase 1 trio + length-floor convergence gate + Mistral 429 warning.

- 2026-06-04 — David's Midea Target product-review run (v3.63.136) and cookie Quick Start run (v3.63.139 / chocolate chip) showed that reasoning-token burn varies with task complexity. DeepSeek-v4-pro burned 34,253 completion tokens for a 2,446-char Midea R1 output (~14x ratio) but only 890 completion tokens for a 533-word cookie R1 (~1.67x ratio). Cohere's 7.4x ratio held across both runs (consistent overhead). Gemini-3.1-pro-preview hides ~1,500-2,800 reasoning tokens per call under usageMetadata.totalTokens (separate from completion_tokens). Hive Profiles arc is the answer — give users a one-click "Cheap" preset that excludes reasoning models entirely.

- 2026-06-04 — release-check validator caught no drift across v3.63.135/.136/.137/.138/.139/.140 ceremonies (six green in a row). ROI confirmed.

- 2026-06-04 — SEO measurement infrastructure live since v3.63.128.

- cleaner.html preserved in David's local working tree.

- Adoption-friction note: David surfaced "zero sales after months of build" 2026-06-01, reaffirmed 2026-06-04. Engineering merit answers stand independent of conversion data.
