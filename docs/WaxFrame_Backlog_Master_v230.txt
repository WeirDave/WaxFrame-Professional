WaxFrame Backlog Master v230
2026-06-05

State: v3.63.161 Pro shipped on main. Second of four planned releases (v3.63.160-163) for the Builder/Worker meld. This release was a full User Manual sweep:

- Sidebar links collapsed from 5 setups to 4 (Setup 1 — Set up your hive, Setup 2 — Project, Setup 3 — Reference Material, Setup 4 — Starting Document)
- Step table renumbered + Setup 1+2 collapsed into one row
- All section titles renamed/renumbered: Setup 1 retitled "Set up your hive"; Setup 2 (Choose Your Builder) replaced with a stub redirect block preserving #step2 anchor; Setup 3/4/5 became 2/3/4; Steps 6-11 decremented to 5-10
- All inline prose references to old numbering updated (Setup 5 → 4, Setup 4 → 3, Setup 3 → 2 with cascade safety)
- "Worker Bees screen" → "Set up your hive screen" in two specific blurbs
- "✗ Builder selected" status-bar bullet rewritten to point at the per-row 🔨 Builder button instead of the retired Builder screen
- Screenshot regeneration FLAGGED but not done (assets still show v3.63.146 layout; prose now describes current layout regardless)

The Builder content from the old standalone Setup 2 section (synthesizer + handyman roles, Notes drawer + Send to Builder workflow, cost notes, model recommendations) is preserved in the #step2 redirect stub so users hitting that anchor still get the essential information.

Lesson from this sweep: replace_all over too-generic strings ("Setup 3 — ") can cascade unintentionally. When doing numbering migrations, replace HIGHEST number first then cascade downward, so each replace only affects intentional targets. Future doc sweeps should use that ordering.

Up next:
- v3.63.162: README.md (alt text + body references + screenshot regen flagging) + landing pages (ai-cover-letter-editor, ai-resume-review, ai-business-proposal, document-playbooks, prompt-editor, templates) + info modals in index.html + api-details.html walkthrough
- v3.63.163: Code cleanup — remove #screen-builder DOM, remove continueFromBuilder legacy stub, retire worker-bees-prototype.html, archive docs/worker-bees-audit.md, CSS dead-selector cleanup

v3.63.160 state still holds: intro paragraph rewrite, Change Builder modal restructure, sidebar+Hive Profile light-mode fix. v3.63.159 state still holds: bee tooltips + top count chip gold.

PRIOR STATE (v3.63.160, retained for context until next release reshuffles the margin):
Intro paragraph rewritten. Change Builder modal switched to row-based methodology (per-row icon + name + Model: label + compact select + Builder button). help.html reviewed — no methodology overlap. Sidebar block + Hive Profile bar light-mode fix (work-screen panel pattern with rgba(0,0,0,0.55) + backdrop-filter + light text in both themes).

PRIOR STATE (v3.63.159, retained for context until next release reshuffles the margin):
Bee mascot tooltips via centralized BEE_NAMES mapping + applyBeeTooltips walker. Top count chip strong elements in var(--accent) gold for consistency with .ai-setup-group-count and bulk-select toolbar.

PRIOR STATE (v3.63.158, retained for context until next release reshuffles the margin):
Sidebar Jump-to-AI scroll regression fix. Replaced window.scrollTo with row.scrollIntoView + scroll-margin-top:80px + requestAnimationFrame timing.

Open Bugs

(none currently tracked outside the active task list)

Open Features / Tasks

- v3.63.162 (next): README.md alt text + body references + landing pages + info modals + api-details
- v3.63.163: code cleanup
- Screenshot regeneration for user manual (screenshot_setup1_dark/light.png, screenshot_setup2_dark/light.png in landing pages) — FLAGGED but not done in v3.63.161
- #10 Hive Profiles: "Save current as named profile" (named user profiles)
- #12 Hive Profiles: dedicated docs page (hive-profiles.html)
- #24 Worker Bees rework: drop sub-1366px responsive code
- #27 Worker Bees rework: 3-group ordering (defaults / customs / servers)
- #35 Auto-classify tier picks always (not click-to-classify)
- #36 Worker Bees rework: server provider group label
- Per-row "Manual override" tag — needs delta tracking
- Per-tier WHY text: classifier currently produces one overall WHY
- WaxFrame_Builder_v3.png pruning candidate
- Restore-from-Checkpoint diff-and-pick UI — PARKED
- Hive parallel fan-out RPS-awareness
- Model recommender filter-awareness warning
- Failover Builder behavior — single-round CONTENT_FILTERED handling
- Builder-incapable family list expansion
- Pause/resume at reviewer seam
- File System Access API exploration
- Provider Catalog foundation (parked)
- CSS rule consolidation sweep
- Repo housekeeping — docs/ folder
- Triage CodeQL + Dependabot findings
- Rules reference updates

Observations

- 2026-06-05 — User Manual sweep took two passes (initial edits + cascade-correction). The lesson logged above (replace highest number first, then cascade) is worth applying to future doc migrations. The manual is the largest single doc file at 2121 lines and touches every step of the user journey, so consistency matters more here than anywhere else.

- 2026-06-05 — The standalone Step 2 redirect stub is the right pattern for retired sections — preserves the anchor for backward compatibility, surfaces the essential preserved content (Builder roles, cost notes), and explicitly points the user to where the functionality now lives. Reusable pattern for any future section consolidations.

- 2026-06-05 — release-check validator caught no drift across v3.63.135 through v3.63.161 (twenty-seven ceremonies).

- 2026-06-05 — SEO measurement infrastructure live since v3.63.128.

- cleaner.html preserved in David's local working tree.

- Adoption-friction note: David surfaced "zero sales after months of build" 2026-06-01. The User Manual was a real friction source — anyone reading it for guidance was finding instructions for a flow that no longer existed. Fixing it removes a credibility loss point.
