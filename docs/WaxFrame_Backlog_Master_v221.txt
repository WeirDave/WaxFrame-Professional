WaxFrame Backlog Master v221
2026-06-04

State: v3.63.152 Pro shipped on main. Worker Bees v3 rework Phase 2 lands: the Hive Profile dropdown above the bee grid. Five options (Custom / 💰 Cheap / ⚖️ Balanced / 🧠 Thinker / ⚡ Fast). Choosing a tier walks aiList and applies the cached tier pick for every keyed AI via saveModelForAI. State persisted to localStorage["waxframe_hive_profile"]. Internet mode only (Server mode AIs don't have tier classification). Built entirely on existing primitives (getCachedTiers / saveModelForAI / renderAISetupGrid / toast) — no new storage shapes, no classifier changes. Advances tasks #9 (storage + resolver) and #11 (UI dropdown).

Companion to v3.63.151 (the 6-card grid). That release made tier picks one-click per AI; this release makes them one-click across all AIs. Together they replace the "hunt in the dropdown" pattern with "pick a recommendation, apply".

David's session reset at 10pm landed cleanly: he killed the previously-scheduled doc-sweep cron at 9:56pm with the prototype screenshot and "skip the crons it's 4 min to 10 anyway" — we worked live on Phase 1 (6-card grid) and Phase 2 (Hive Profile dropdown) in sequence after the reset. David also explicitly removed the "ask which next?" friction by saying "I don't like making choices like this You make the choices" — captured in [[feedback-make-the-call-dont-ask]] for future sessions.

v3.63.151 state still holds: Worker Bees v3 Phase 1 (6-card grid in expanded row). v3.63.150 state still holds: collapsed-row cleanup (Builder chip between name and model, gap tightened, pencil reseated). v3.63.149 state still holds: Builder Bee asset swap (v4.png natively left-facing).

PRIOR STATE (v3.63.151, retained for context until next release reshuffles the margin):
6-card grid (✨ Reviewer + 🔨 Builder + 💰⚖️🧠⚡ tiers) renders in each AI's expanded row. _renderHiveRolesAndTiers / swapAIModelFromHiveCard wired to saveModelForAI + renderAISetupGrid. Active card shows "✓ In use" green border + disabled button.

PRIOR STATE (v3.63.150, retained for context until next release reshuffles the margin):
Worker Bees collapsed-row cleanup. .ai-setup-name-group inline-flex with gap:4px. Pencil button padding-x and margin-left zeroed.

PRIOR STATE (v3.63.149, retained for context until next release reshuffles the margin):
Builder Bee asset swap. WaxFrame_Builder_v4.png replaces v3.png in the header.

Open Bugs

(none currently tracked outside the active task list)

Open Features / Tasks — Worker Bees rework Phase 3+

- #10 Hive Profiles: "Save current as named profile" (named user profiles); SHOULD wait for delta-tracking UX
- #12 Hive Profiles: dedicated docs page (hive-profiles.html)
- #23 Worker Bees rework: icon legend + light-mode contrast pass — requires sidebar chassis change
- #24 Worker Bees rework: drop sub-1366px responsive code — cleanup pass
- #25 Worker Bees rework: jump-to-AI scroll-with-offset — pairs with #23 sidebar
- #26 Worker Bees rework: toolbar alignment — Recommend All AIs button — partially covered by v3.63.152's profile bar; remaining: visual alignment between profile bar + controls row
- #27 Worker Bees rework: 3-group ordering (defaults / customs / servers) — current Internet/Server toggle means defaults+customs share a page, servers another; prototype's single-page 3-group view is a bigger restructure
- #32 Builder consolidation: documentation sweep (User Manual, README, landing pages, info modals)
- #34 Worker Bees rework: Builder education in 3 surfaces — modal + standalone page remaining
- #35 Auto-classify tier picks always (not click-to-classify)
- #36 Worker Bees rework: server provider group label
- Per-row "Manual override" tag — needs delta tracking; deferred from v3.63.152
- Per-tier WHY text: classifier currently produces one overall WHY; could split per-tier
- WaxFrame_Builder_v3.png pruning candidate once worker-bees-prototype.html is updated or retired
- Restore-from-Checkpoint diff-and-pick UI — PARKED
- Hive parallel fan-out RPS-awareness
- Model recommender filter-awareness warning
- Failover Builder behavior — single-round CONTENT_FILTERED handling
- Builder-incapable family list expansion
- Pause/resume at reviewer seam
- File System Access API exploration
- Provider Catalog foundation (parked)
- CSS dead-selector cleanup branch
- CSS rule consolidation sweep
- Repo housekeeping — docs/ folder
- Triage CodeQL + Dependabot findings
- Rules reference update — "verify every external URL before shipping" + "always update const BUILD in addition to the // Build comment" + "release-check validator should compare const BUILD against // Build comment"

Observations

- 2026-06-04 — v3.63.151 + v3.63.152 are a tightly-paired pair. The 6-card grid surfaces the per-AI recommendations; the Hive Profile dropdown applies them in bulk. Either alone is incomplete: the cards without the dropdown require N clicks for N AIs; the dropdown without the cards hides the option to override individual rows. Shipping them in sequence (within minutes of each other on the same evening) keeps the conceptual model intact.

- 2026-06-04 — Both phases reused existing data plumbing entirely. The classifier (v3.63.141) and recommender (v3.32+) already populate the caches that Phases 1 and 2 read. The architectural decisions in the early Hive Profiles work paid off — UI added without touching any networking, storage shapes, or classification logic.

- 2026-06-04 — Auto-mode + David's "make the call" memory ([[feedback-make-the-call-dont-ask]]) eliminated the "which next?" friction. Phase 2 started immediately after Phase 1 landed, with the scope picked + one-line reason ("most visible follow-up; complements the per-AI 6-card grid with bulk apply") rather than a menu of options.

- 2026-06-04 — release-check validator caught no drift across v3.63.135 through v3.63.152 (eighteen ceremonies).

- 2026-06-04 — SEO measurement infrastructure live since v3.63.128.

- cleaner.html preserved in David's local working tree.

- Adoption-friction note: David surfaced "zero sales after months of build" 2026-06-01. v3.63.146 (cost callouts) → v3.63.147 (setup-flow simplification) → v3.63.148-149 (brand mascot placement) → v3.63.150 (row visual polish) → v3.63.151 (6-card grid) → v3.63.152 (Hive Profile dropdown) is a six-release single-day arc dominantly focused on conversion-friction reduction. Each release addresses a distinct moment in the user's first-time path; together they should significantly lower the cognitive load between "I want to try this" and "I'm refining a document".
