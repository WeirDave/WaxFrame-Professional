WaxFrame Backlog Master v205
2026-06-04

State: v3.63.136 Pro shipped on main. Three new per-platform Product Review templates — Walmart / Target / Newegg — added to js/templates.js based on David's verified constraints from each platform's review submission form (2026-06-04). Walmart: 50-char title cap (tightest of any platform), 3,000-char body cap, body explicitly addresses five secondary star prompts (expectations / value / fit / ease of installation / quality). Target: no title cap (soft 100-char ceiling), 3,500-char body cap (UI claims 4,000 but accepts ~3,500), body addresses three star prompts (ease of use / quality / value). Newegg: structurally different — three separate input fields (Pros / Cons / Overall Review), each capped at ~4,000 chars (UI claims 5,000 but accepts ~4,376). Newegg template emits STRUCTURED OUTPUT with three exact-text section headers (PROS: / CONS: / OVERALL REVIEW:) so the user copy-pastes each labeled section into Newegg's three corresponding input fields. Uses eggs not stars (1-5 eggs). Per-platform recipe reference block in templates.js updated with real verified numbers replacing prior TBDs; secondary-star-rating prompts each platform asks for are also documented. document-playbooks.html length-recipe table updated with template anchor links for each new platform. Best Buy row remains TBD pending real measured constraints. Validator stayed green through the ceremony — no package.json drift this time (lesson learned from v3.63.135 ceremony where the validator caught the miss).

v3.63.135 state still holds: templates page quick wins (Amazon length recipe reference + per-card Duplicate + Recently-saved sort). v3.63.134 state still holds: GitHub Actions release-ceremony validator. v3.63.133 state still holds: Deep Dive capture dual-homed. v3.63.132 state still holds: Builder-incapable model filter (Jamba) + BUILDER_TRUNCATED. v3.63.131 state still holds: audit polish. v3.63.130 state still holds: selective EXPORT modal + scrubbed retirement. v3.63.129 state still holds: Backup → Checkpoint rename. v3.63.128 state still holds: 17-playbook body-copy link sweep. Sitemap lastmod 2026-06-04. PLAYBOOK TEST MASTER v37 is the live test-tracking doc.

PRIOR STATE (v3.63.135, retained for context until next release reshuffles the margin):
Templates page quick wins. (1) Per-platform recipe reference block in js/templates.js before product-review-amazon (canonical headline + body length recipes for every platform we know constraints for). (2) Per-card 📋 Duplicate action on My Templates customs — clones with auto-incrementing "(copy)" naming. (3) "Recently saved" sort dropdown on My Templates (appears with 2+ saved templates) — default newest first, Alphabetical alternative. Session-scoped preference.

PRIOR STATE (v3.63.134, retained for context until next release reshuffles the margin):
GitHub Actions release-ceremony validator (.github/workflows/release-check.yml + tools/release-check.mjs). First run caught 18 silently-failing CSS token references — all fixed in the same release. Validator caught its first real-world drift during v3.63.135 ceremony (package.json forgot to bump).

PRIOR STATE (v3.63.133, retained for context until next release reshuffles the margin):
Deep Dive capture dual-homed onto Settings → Diagnostics + a one-click "Enable Deep Dive" button on help.html. Dev-toolbar 🔬 toggle kept for fast access. All three surfaces sync the same localStorage flag.

PRIOR STATE (v3.63.132, retained for context until next release reshuffles the margin):
Builder-incapable model filter (Jamba) + BUILDER_TRUNCATED error code. New BUILDER_INCAPABLE_FAMILIES regex wired into filterModelsForRole(); ⚠️ Reviewer-only badge in buildModelSelector(); detection at the missing-conflict-block sites routes finishReason LENGTH/MAX_TOKENS to the new code. Docs in ai-api-pricing.html + user-manual.

PRIOR STATE (v3.63.131, retained for context until next release reshuffles the margin):
Audit polish on v3.63.130. --modal-w-md → --modal-w-base; --space-22 → --space-24; removed dead leftLocalIDBAlone; #settings anchor for "Save Checkpoint scope" reworded to #checkpoint-scope.

PRIOR STATE (v3.63.130, retained for context until next release reshuffles the margin):
Phase B of the Checkpoint arc — selective EXPORT modal + retirement of the Save Checkpoint (Scrubbed) flow. Six-checkbox modal (Project / Session / Hive / Keys / Builder / License). Backup format v4 → v5 with _waxframe_backup_scope envelope.

Open Bugs

(none currently tracked)

Open Features

- Restore-from-Checkpoint diff-and-pick UI — turn the current blunt overwrite into a comparison + selective-import flow. STATUS: PARKED — gated on user feedback once the selective-export modal has actual users.

- Custom Templates Phase 2 + Phase 3 — full editors behind the scaffold buttons that landed in v3.63.97. PHASE 2 (Create Blank), PHASE 3 (Import .json), PHASE 1B (hive auto-reconcile on apply).

- Templates page Phase 1 follow-ups — REMAINING: search/filter bar, per-card View Details expander, bulk Export All customs as ZIP. SHIPPED in v3.63.135: per-card Duplicate action + Recently-saved sort.

- Mistral 429 proactive warning via X-RateLimit-Remaining header. Mistral's API returns the header; WaxFrame doesn't currently read it. Surface a soft warning BEFORE a 429 hits (e.g. yellow chip on the Mistral row "~X requests left this minute"). Likely needs a small per-provider hook in callAPI (app.js ~11500) since rate-limit headers differ by provider.

- Hive parallel fan-out RPS-awareness. Optional per-provider concurrency cap so paid Mistral on a low tier doesn't 429 itself with parallel fan-out.

- Model recommender filter-awareness warning — when the recommender's pick has been flagged in the current session's ringBuffer with CONTENT_FILTERED, surface a ⚠️ next to that model in the Change Builder dropdown.

- Failover Builder behavior — primary Builder hits CONTENT_FILTERED / EMPTY_RESPONSE → auto-failover to a configured Failover Builder for that one round only. Settings → Auto Mode dropdown exists; this entry is about the BEHAVIOR for single-round CONTENT_FILTERED (today the dropdown only fires on failure-streak halts).

- Builder-incapable family list expansion — watch-list. Currently just Jamba. As other broken-as-Builder families surface, extend BUILDER_INCAPABLE_FAMILIES regex.

- Pause/resume at reviewer seam — finer-grained pause INSIDE a round, between reviewers finishing and the builder firing.

- File System Access API exploration — true file persistence vs download pile-up. Browser-support: Chrome/Edge yes, Safari/Firefox no.

- Provider Catalog + Hive Profiles Foundation — two-layer provider model. Sequencing: (1) `starter: true` flag on API_CONFIGS entries. (2) "+ Add from Catalog" modal. (3) Catalog expansion in two batches: Cloud API (Perplexity/Cohere/Together) then Local Server (LM Studio/Ollama/Alfredo/Open WebUI). (4) Hive Profiles as saved snapshots.

- CSS dead-selector cleanup branch — ~155 flagged selectors. Static grep insufficient (display:contents is load-bearing, JS template-literal class names evade grep). Dedicated branch, phased deletion, visual smoke test gate.

- CSS rule consolidation sweep — 6 cases of same-component-language-multiple-custom-classes worth consolidating.

- Product Review per-platform templates — REMAINING: Best Buy. Constraints not fully verified yet (title cap unknown, body sweet-spot range unknown without measured run on the form). SHIPPED in v3.63.136: Walmart / Target / Newegg.

- Repo housekeeping — `docs/` folder. v30 dropped 2026-06-04. Continued trim as needed.

- Triage CodeQL + Dependabot findings once first scans complete. Blocked on David pulling Security tab findings.

Observations

- SHIPPED — Walmart / Target / Newegg per-platform Product Review templates (v3.63.136). Closes three of the four "Product Review per-platform templates" items from prior backlogs. Best Buy remaining pending real measured constraints. Reference block in templates.js now carries the real verified numbers David captured directly from each platform's review submission form on 2026-06-04. Newegg's structured-output pattern (PROS: / CONS: / OVERALL REVIEW: section headers) is a new variant of the template format — the Builder emits three labeled sections, user copy-pastes each into Newegg's three input fields.

- 2026-06-04 (during v3.63.135 ceremony) — the v3.63.134 release-check validator caught its FIRST REAL DRIFT IN THE WILD. Cache-bust sweep updated everything except package.json (regex pattern matched ?v= but not "version":). Validator surfaced "package.json version 3.63.134 != APP_VERSION 3.63.135" on the pre-commit run. One-line fix, then green. v3.63.136 ceremony stayed green throughout — lesson learned. ROI confirmed in <2h.

- 2026-06-04 (post-v3.63.134) — Length-floor convergence note from David's Trim to TripAdvisor run. Majority convergence fired at R3 with the document at 478 words against a 500-word floor (Range mode, 500–900 words). Two R3 holdouts (ChatGPT, DeepSeek) explicitly proposed expanding the recommendation paragraph to clear the floor; convergence engine fired anyway because the majority view was "done." Investigation needed: the convergence-path length check at app.js:14555 and the matching unanimous path DO call lengthGuardPrompt({ kind: 'convergence_under' }) when status is not 'ok', so the prompt path IS wired up. David's checkpoint shows lengthGuardOverride: false. Either an off-by-one in getLengthStatus, the prompt fired but David clicked through without registering it, or a sub-state of the majority-with-holdouts path bypasses the prompt. Re-add as a formal Open Bug if investigation confirms a code-path miss.

- 2026-06-04 — SEO measurement infrastructure live since v3.63.128. Google Search Console + Bing Webmaster Tools imported. First signals expected ~2 weeks from setup.

- cleaner.html (offline backup-cleaner tool) preserved in David's local working tree as a working reference implementation for the eventual in-app normalizeHive() port (if real users ever surface with legacy ugly-ID backups). NOT committed to repo — no production value.

- Adoption-friction note: David surfaced "zero sales after months of build" 2026-06-01, reaffirmed 2026-06-04. Engineering merit answers stand independent of conversion data. Funnel hasn't been opened yet; conversion is untested, not failed.
