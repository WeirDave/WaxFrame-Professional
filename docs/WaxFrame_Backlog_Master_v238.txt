WaxFrame Backlog Master v238
2026-06-06

Open Bugs

- Lock button one-way — does not unlock
  Per-row applied-change lock in the Conflicts panel engages but appears
  unable to toggle back. Suspect lockAppliedChange at app.js:18485.
  v3.63.165 diagnostic console.log('[wf:lock]', ...) at app.js:18504 is
  in place. Static analysis found no obvious cause. Three runtime
  hypotheses ranked by likelihood:
    1. change.locked is false at click time (rendered state vs data
       state diverged — most likely if history got reassigned).
    2. UNLOCK branch enters correctly but the silent return at
       app.js:18514 fires because change.new became empty.
    3. UNLOCK branch runs cleanly but document.getElementById(
       'conflictsPanel') returned null inside renderConflicts and it
       silently bailed.
  DEFERRED until next user repro with [wf:lock] console output.


Open Features

- Restore-from-Checkpoint diff-and-pick UI — PARKED
- Hive parallel fan-out RPS-awareness
- Failover Builder behavior — single-round CONTENT_FILTERED handling
- Builder-incapable family list expansion
- Pause/resume at reviewer seam
- File System Access API exploration
- Provider Catalog foundation — PARKED
- CSS rule consolidation sweep — PARTIAL
  Modal/hdr/close/body folded into base in v3.63.190. Remaining
  duplicates: .template-gallery-sidebar / -sidebar-label / -sidebar-
  link, .template-path-card, .template-card / .template-card.is-
  recommended / .template-card-name / .template-card-desc,
  .template-gallery-section / .template-gallery-section-title, and
  the .template-card-edit / -dup / -export / -del / -wrap edit-action
  button block at style.css:14765+. Each remaining group needs per-
  selector tracing because the cascade interacts with the
  [data-theme="dark"] specialization at style.css:14220+. Browser-
  verification session.
