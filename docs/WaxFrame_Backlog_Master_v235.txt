WaxFrame Backlog Master v235
2026-06-06

State: v3.63.183 Pro shipped on main. Project Templates modal rewritten to clean theme-variable surfaces (var(--surface) for the box and body, var(--surface2) for the gallery header strip and the path indicator, var(--text) / var(--text-dim) for typography, var(--border2) for the close-button border) — no honeycomb tile, no rgba dark-glass overlay, no backdrop-filter blur. Whites and light grays in light mode, blacks and dark grays in dark mode; both themes mirror each other structurally. Dark-mode card hover converted from rgba(245,166,35,0.10) full-replacement to a 10% amber tint layered over a solid #151927 so the cards stay opaque on hover regardless of what's behind them. Perplexity MODEL_FALLBACKS list bumped from 4 to 5 Sonar models (sonar-reasoning was missing; the discovery prompt's own example in api.js:444 already lists all 5). cleaner.html standalone utility removed — first paid user (Andy, RFI work) does not have the Dropbox-legacy custom-AI ID collision bug. Build stamp and cache-bust query string swept across all HTML/JS so the CDN and browsers actually pick up the work that previously shipped against the stale ?v=3.63.182 key.

PRIOR STATE (v3.63.182, retained for context until next release reshuffles the margin):
Project Templates modal and public Template Catalog received documentation-aligned visual polish. Light mode used a white header, soft document canvas, white cards, amber rails, denser section labels. Dark mode received the same structural treatment with separated charcoal cards and amber rails. The honeycomb-glass header pattern introduced here is what v3.63.183 reverted; the document-card structure stays.

PRIOR STATE (v3.63.164, retained for context until next release reshuffles the margin):
Live viewport readout added to min-screen-overlay so users see the actual failure mode (CSS-pixel viewport, not display resolution). Inline IIFE that runs immediately (NOT deferred to app.js DOMContentLoaded because users hitting the overlay are by definition users whose viewport is too small for the full app to load). Resize + visualViewport.resize listeners for live updates including zoom and DPI changes. Hint flips green ("above the minimum") vs red ("needs N px wider and M px taller"). DPI scaling guidance added to the resize-help paragraph.

Inspired by a real David user incident: user's display was 1920x1080 but browser viewport was restricted somehow, had to switch browsers to fix. A "what is my screen resolution" link would have misdiagnosed; the inline viewport readout points at the actual problem.

CLOSED in the 2026-06-06 audit (were on v234, found already shipped):

- Notes button cut off at smaller effective viewport (DPI scaling case) — CLOSED.
  Two pieces shipped that together handle this:
    * v3.63.164 — Live viewport readout in min-screen-overlay (index.html:2698-2718) plus explicit DPI guidance in the body text: "if your screen resolution looks fine but you're still blocked, check your browser zoom level (Ctrl+0 to reset) and Windows DPI scaling (Settings → Display → Scale; 200% halves your effective viewport)". Users get a self-diagnostic when their effective viewport is below the gate.
    * v3.63.166 — Density pass at style.css:2742 explicitly responding to David's "at 1366×768 the Notes button label is truncated to 'tes'" report. @media (max-width: 1500px) collapses top-bar buttons to icon-only with tooltip labels. Notes button no longer clips at the band between min-screen gate and ideal viewport.
  v234 was written before these were re-checked; carry-over.

- Lock button initial state inversion — CLOSED.
  v3.63.165 work established the convention "icon shows CURRENT state, label shows ACTION on click" across all four lock toggles. Re-verified 2026-06-06:
    * Per-row Applied Change (app.js:17404-17405): unlocked → "🔓 Lock this line"; locked → "🔒 Unlock"
    * Bulk Lock All / Unlock All (app.js:17377-17378): unlocked → "🔓 Lock All"; locked → "🔒 Unlock All"
    * Conflict Decision UI (app.js:18021): unlocked → "🔓 Lock my selection"; locked → "🔒 Unlock"
    * lockConflictDecision toggle (app.js:17490): same pattern via decision-lock-btn-locked class
  The convention is consistent: open padlock at rest, closed padlock when in locked state. v234 was unaware this had landed.

QUEUED in this backlog (not yet addressed):

Open Bugs

- Lock button one-way — does not unlock
  - Clicking the lock button engages lock state but appears unable to toggle back to unlocked per David's report
  - Top suspect: per-row applied-change lock in Conflicts panel (lockAppliedChange at app.js:18485)
  - 2026-06-06 status: static analysis pass found no obvious cause. The function HAS both LOCK and UNLOCK branches and the toggle is gated on change.locked. renderConflicts reads from the same history array the function mutates in place. No outer click interceptors on .applied-card. The v3.63.165 diagnostic console.log('[wf:lock]', ...) at app.js:18504 is in place but won't tell us anything without runtime reproduction. Three runtime hypotheses ranked by likelihood:
    1. change.locked is false at click time (rendered state vs data state diverged — most likely if history got reassigned between clicks)
    2. UNLOCK branch enters correctly but the silent return at app.js:18514 fires because change.new became empty
    3. UNLOCK branch runs cleanly but document.getElementById('conflictsPanel') returned null inside renderConflicts and it silently bailed
  - DEFERRED until next user repro with [wf:lock] console output. David indicated 2026-06-06 he will keep using the feature and watch for the bug.

Open Features

- Custom template categories
  - When the user saves a project as a custom template, let them assign a category — same category list as the built-in templates (Career & Hiring, Business & Sales, Content & Marketing, Personal & Everyday, Reviews & Recommendations) OR a free-form custom category
  - Render the categories in the Template Gallery sidebar nav for the Custom Templates view, just like the scratch/refine sidebars — so users with 10+ custom templates can navigate by category rather than scrolling a flat list
  - Currently all custom templates land in a single "My Templates" bucket; the sidebar nav is skipped on the custom path because there's only one bucket. With categories, the sidebar becomes useful and the custom view matches the built-in view shape
  - Requires: schema bump on the custom template JSON (add category field, backward-compat default for existing entries), UI in Save-as-Template modal for category picker, conditional sidebar rendering in renderTemplateGalleryBody for path == custom when category count > 1

- Screenshot regeneration for README + User Manual (FLAGGED, not done)
- #10 Hive Profiles: "Save current as named profile"
- #12 Hive Profiles: dedicated docs page (hive-profiles.html)
- #24 Worker Bees rework: drop sub-1366px responsive code
- #27 Worker Bees rework: 3-group ordering (defaults / customs / servers)
- #35 Auto-classify tier picks always
- #36 Worker Bees rework: server provider group label
- Per-row "Manual override" tag — needs delta tracking
- Per-tier WHY text: classifier currently produces one overall WHY
- Restore-from-Checkpoint diff-and-pick UI — PARKED
- Hive parallel fan-out RPS-awareness
- Model recommender filter-awareness warning
- Failover Builder behavior — single-round CONTENT_FILTERED handling
- Builder-incapable family list expansion
- Pause/resume at reviewer seam
- File System Access API exploration
- Provider Catalog foundation (parked)
- CSS rule consolidation sweep
  - 2026-06-06 note: at least two duplicate definitions of .template-gallery-modal, .template-gallery-hdr, .template-gallery-body in style.css — the original at lines 13603-13745 and the v3.63.182 polish block at lines 14089-14111. The later block intentionally overrides. Same pattern likely exists for other heavily-iterated selectors. Sweep would consolidate to single definitions.
- Repo housekeeping — docs/ folder
- Triage CodeQL + Dependabot findings
- Rules reference updates

Observations

- 2026-06-06 — v3.63.183 was net cleanup. Stripped the v3.63.180-182 Project Templates honeycomb experiment back to the existing theme surface ramp. Three iterations earlier in the session (honeycomb-in-header → honeycomb-as-modal-body → flat var(--surface) surfaces) walked back to the simplest theme that matched the rest of the app. Lesson: when the theme stack already has a working surface ramp, prefer it over inventing modal-specific texture treatments.

- 2026-06-06 — The honeycomb body background on the page (html/body, .hp-section-header, work-screen panels) stays. Only the Templates modal lost it. Different surface, different needs: the page can carry the texture because section headers float over it as glass strips; a modal can't because the modal overlay covers the body so there's nothing to show through. Keep the texture where the structural pattern supports it.

- 2026-06-06 — The cache-bust query string ?v=3.63.182 DRIFTED for the entire v3.63.180-182 cycle. Each release shipped CSS/JS changes against the same key. End result: CDN and browser caches kept serving the previous file even after deploy; David spent at least one session reverting work he thought was broken when the actual issue was stale cache. v3.63.183 bumped the key as part of stamping the work. Future point releases need to bump it every time CSS/JS lands, not just on the next "ceremonial" release. Worth adding to the release-check validator if not already there: if any of style.css / js/*.js changed since the last tagged release, the ?v= query in HTML must have changed too.

- 2026-06-06 — Andy is the first paid user (purchased 2026-06-05 at work, RFI use case). cleaner.html removed in the same release because the bug it repaired only ever affected David's own Dropbox-legacy custom-AI ID collisions; Andy has no such state.

- 2026-06-05 — Template Gallery theme polish needs both theme passes every time. v3.63.182 started as a dark-leaning patch, and David caught that light mode had not received the same care. Future visual work should verify light, dark, and auto-light/auto-dark before ceremony.

- 2026-06-05 — The amber rail system should stay structurally identical across themes. For the template/path cards the shared rule is `border-left: 4px solid ...`; theme overrides should change the color and surrounding surface, not the width.

- 2026-06-05 — The viewport-vs-screen-resolution confusion is the #1 source of "but my screen is 1920x1080 so why am I blocked" support friction. Surfacing the actual CSS-pixel viewport in the overlay disambiguates the diagnosis at zero cost — no external dependency, no link rot, no second-page-load. Should have been there from day one of the min-screen-overlay; flagged here as a lesson for any future "what is your environment" gating.

- 2026-06-05 — Inline IIFE pattern (script block in the page body, not deferred to app.js) is the correct architectural choice for any feature that needs to work BEFORE the full app loads. The min-screen-overlay overlay is the canonical example: users hitting it can't load the app, so the diagnostic UI has to be self-contained. Same pattern would apply to any "browser not supported", "JS disabled", "cookies blocked" gating in the future.

- 2026-06-05 — release-check validator caught no drift across v3.63.135 through v3.63.164 (thirty ceremonies).

- 2026-06-05 — SEO measurement infrastructure live since v3.63.128.

- Adoption-friction note: David surfaced "zero sales after months of build" 2026-06-01. As of 2026-06-06, Andy is the first paid user. The viewport-readout fix (v3.63.164) removes a "user got bounced and didn't know why" support burden. Every user that gets bounced AND can't diagnose it is a likely lost conversion. The new readout + DPI guidance keeps them on the page with actionable next steps.
