WaxFrame Backlog Master v208
2026-06-04

State: v3.63.139 Pro shipped on main. Hive Profiles arc kicks off — step 1 lands the dynamic per-provider tier classifier (no static MODEL_TIERS block; each provider's own AI classifies its current model lineup into CHEAP / BALANCED / THINKER / FAST slots on demand). Surfaces only in the dev toolbar (🐝 Classify Tiers button, results land in the new 📦 Bundle for Scout download). Classification prompt explicitly normalizes NONE as expected + correct for single-model providers / lineups with no reasoning variant / lineups with no budget tier, and anchors on user-cost as the reason a wrong answer matters — designed to dodge the v3.32.10 FASTEST/BUDGET failure mode. Per-provider cache (waxframe_tiers_<provider>) mirrors the recommender cache shape; classifications survive reload, refreshable via the dev-toolbar button. Same release also hard-simplifies the dev-mode diagnostic surface: Deep Dive auto-enables on dev-mode unlock; the View Captures modal (and its 6 actions) + Tiers viewer modal both retired in favor of a single 📦 Bundle for Scout button that downloads ringBuffer + tier classifications + project checkpoint + version metadata in one JSON file. Clear Buffer button preserves tier classifications (per-provider, not per-project). Step 2 (Perplexity verification) lands next, then storage/resolver/dropdown UI/docs page in steps 3-4.

David's directional pivot from v207's "Custom Templates Phase 2" parking spot: Phase 2/3/1b of Custom Templates already shipped in v3.63.43/.44/.45 (the rules-locked design uses the real setup screens as the editor). Hive Profiles became the next concrete arc instead — surfaced by the v3.63.136 Midea-AC review run where DeepSeek's reasoning model burned 34,253 completion tokens against a 2,446-char output and Cohere burned 3,257 tokens to emit "NO CHANGES NEEDED." The fix isn't to disable those models — their reasoning output is the feature — but to give users a one-click way to swap entire hive lineups based on the trade-off they want (cheap vs. deep, fast vs. capable).

v3.63.138 state still holds: Templates page Phase 1 trio complete (search/filter + View Details + Export All ZIP) + majority-convergence length-floor gate + Mistral 429 proactive warning. v3.63.137 state still holds: per-platform Product Review variant arc complete (Best Buy added). v3.63.136 state still holds: Walmart / Target / Newegg per-platform templates. v3.63.135 state still holds: templates page quick wins. v3.63.134 state still holds: GitHub Actions release-ceremony validator. v3.63.133 state still holds: Deep Dive capture dual-homed (dev toolbar + Settings → Diagnostics + help.html). v3.63.132 state still holds: Builder-incapable model filter (Jamba) + BUILDER_TRUNCATED. Sitemap lastmod 2026-06-04. PLAYBOOK TEST MASTER v37 is the live test-tracking doc.

PRIOR STATE (v3.63.138, retained for context until next release reshuffles the margin):
Templates page Phase 1 follow-ups complete the trio (search/filter bar pinned over the catalog, per-card View Details expander surfacing the goal fields the hive uses, bulk Export All customs as .zip via lib/jszip.min.js). Majority-convergence length-floor gate at app.js:14449 — when project has a length constraint and the document is out of range AND there are holdouts, MAJORITY convergence is deferred so the holdouts get another round (closes David's TripAdvisor R3 observation). Mistral 429 proactive warning at app.js:15249 — soft warning + yellow ~N-req-left chip on the bee card when remaining < 3.

PRIOR STATE (v3.63.137, retained for context until next release reshuffles the margin):
Best Buy product review template added (product-review-bestbuy). PER-PLATFORM VARIANT ARC NOW COMPLETE — all five US-major retail platforms covered: Amazon (v3.63.94 baseline), Walmart/Target/Newegg (v3.63.136), Best Buy (v3.63.137).

PRIOR STATE (v3.63.136, retained for context until next release reshuffles the margin):
Three new per-platform Product Review templates — Walmart / Target / Newegg — based on David's verified constraints.

Open Bugs

(none currently tracked)

Open Features

- Hive Profiles step 2: Perplexity verification pass. After all providers self-classify, ship the bundle to Perplexity with a web-search verification prompt that confirms / revises / flags-as-unknown each pick against current provider pricing pages. NEXT UP.

- Hive Profiles step 3: storage + resolver + seeded starter profiles (Cheap / Balanced / Heavy thinkers / Speed-first) + custom user-named profiles. localStorage key waxframe_hive_profiles, replace-not-augment semantics, honors current hive-size flexibility (2 / 6 / 10).

- Hive Profiles step 4: dropdown UI above the bee grid + hive-profiles.html docs page (one section per starter profile explaining what it means and when to pick it).

- Restore-from-Checkpoint diff-and-pick UI — turn the current blunt overwrite into a comparison + selective-import flow. STATUS: PARKED — gated on user feedback once the selective-export modal has actual users.

- Hive parallel fan-out RPS-awareness. Optional per-provider concurrency cap so paid Mistral on a low tier doesn't 429 itself with parallel fan-out. v3.63.138 adds passive warning; this is the active throttle counterpart.

- Model recommender filter-awareness warning — when the recommender's pick has been flagged in the current session's ringBuffer with CONTENT_FILTERED, surface a ⚠️ next to that model in the Change Builder dropdown.

- Failover Builder behavior — primary Builder hits CONTENT_FILTERED / EMPTY_RESPONSE → auto-failover to a configured Failover Builder for that one round only.

- Builder-incapable family list expansion — watch-list. Currently just Jamba.

- Pause/resume at reviewer seam — finer-grained pause INSIDE a round, between reviewers finishing and the builder firing.

- File System Access API exploration — true file persistence vs download pile-up.

- Provider Catalog foundation — two-layer provider model. 4-phase sequence: starter flag → "+ Add from Catalog" modal → catalog expansion (Cloud then Local). (Hive Profiles half of the original v207 "Provider Catalog + Hive Profiles Foundation" item is the active arc above; the Provider Catalog half remains parked.)

- CSS dead-selector cleanup branch — ~155 flagged selectors. Static grep insufficient; dedicated branch, phased deletion, visual smoke test gate.

- CSS rule consolidation sweep — 6 cases worth consolidating.

- Repo housekeeping — `docs/` folder. Continued trim as needed.

- Triage CodeQL + Dependabot findings once first scans complete. Blocked on David pulling Security tab findings.

(Note: "Custom Templates Phase 2 + Phase 3" REMOVED from Open Features — investigation in v3.63.139 session confirmed v3.63.43/.44/.45 already shipped Phase 1b/2/3, and the rules-locked design uses the real setup screens as the editor. The "scaffold buttons" wording in v207 was misleading carry-over; nothing to build there.)

Observations

- SHIPPED — Hive Profiles step 1: dynamic per-provider tier classifier (v3.63.139). Dev-mode only for now; user-facing UI lands once classifications validate across keyed providers. Key design choice surfaced by reading v3.32.10's rollback comment: the prior FASTEST/BUDGET prompt asked AIs to recall absolute pricing from memory, which they can't do. v3.63.139's prompt instead asks them to classify a concrete numbered list by name-convention heuristics (mini/haiku/flash → cheap, opus/pro/reasoning → thinker) — list-first classification, not memory recall.

- SHIPPED — Dev-toolbar simplification (v3.63.139). Retired the View Captures modal (Deep Dive viewer with seed/copy/save-DeepDive/save-Checkpoint/clear/refresh actions) — David's workflow was always Save DeepDive → Clear buffer → send-to-Scout, never view-in-place. Replaced by a single 📦 Bundle for Scout button that downloads the ring buffer + tier classifications + project checkpoint + version metadata in one JSON. Deep Dive auto-enables when dev mode unlocks so the toggle stops being friction; Settings/help.html mirrors stay for non-dev users. ~150 lines of unused UI code removed from wf-debug.js.

- 2026-06-04 — David's Midea Target product-review run (v3.63.136) showed DeepSeek-v4-pro burning 34,253 completion_tokens for a 2,446-char R1 output (~14x ratio), DeepSeek burning 2,686 completion_tokens to emit "NO CHANGES NEEDED" at R2, and Cohere burning 3,257 completion_tokens to emit the same three words at R4. Reasoning-tier models bill all hidden CoT thinking as output tokens. The pattern was already flagged for Cohere in Playbook v37; this run shows it's broader (DeepSeek + Gemini-3.1-pro-preview also). Hive Profiles arc is the answer — give users a one-click "Cheap" preset that excludes reasoning models, instead of trying to disable reasoning per-model (which providers don't expose).

- SHIPPED — Templates page Phase 1 trio complete (v3.63.138). Phase 1 arc closes.

- SHIPPED — Majority-convergence length-floor gate (v3.63.138). Closes David's 2026-06-04 TripAdvisor observation.

- SHIPPED — Mistral 429 proactive warning (v3.63.138).

- SHIPPED — Per-platform Product Review template arc complete (v3.63.137).

- 2026-06-04 (during v3.63.135 ceremony) — release-check validator caught its FIRST REAL DRIFT IN THE WILD (package.json version mismatch). v3.63.136 / .137 / .138 / .139 ceremonies all stayed green throughout.

- 2026-06-04 — SEO measurement infrastructure live since v3.63.128. First signals expected ~2 weeks from setup.

- cleaner.html preserved in David's local working tree for the eventual in-app normalizeHive() port. Not committed to repo.

- Adoption-friction note: David surfaced "zero sales after months of build" 2026-06-01, reaffirmed 2026-06-04. Engineering merit answers stand independent of conversion data.
